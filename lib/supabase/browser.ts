/**
 * @deprecated Import from `@/lib/supabase/client` instead.
 * Kept so existing paths resolve during migration.
 */

export {
  createSupabaseBrowserClient,
  createSupabaseClient,
  getSupabaseAccessToken,
} from "@/lib/supabase/client";
