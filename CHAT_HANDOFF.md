# DeptSync Hub — Chat Handoff

## Latest (2026-09-08)
- **ROSTER-EDIT-001 — EDIT MEMBER WORKFORCE DETAILS — CODE REPAIR COMPLETE (NO MIGRATION) / SAMSUNG FIELD ACCEPTANCE PENDING. NOT COMMITTED — NOT PUSHED.** *Delete → Recreate is no longer the correction path for a member's basic information.* **THREE FIELDS SHIPPED — name, phone, `floor_title`. HOME DEPARTMENT DELIBERATELY NOT BUILT.** **(1) STOP CONDITION 1 DID NOT FIRE — job title does not touch platform role, and the repository proves it twice.** `resolveRosterJobSave` maps a job option to `role` **and** `floor_title`, but the dependency is **one-directional**: role decides whether a floor title exists at all (`null` for Supervisor / MasterAdmin), and **all four titles — Specialist, CSA, Cashier, Receiving — are associate-level options**; `DEPT_SYNC_STATE.md:192` already recorded `floor_title` as *"orthogonal to platform role"*. The route **never writes `role`** and **refuses a title on a non-associate** rather than creating a state the create flow cannot produce. **The coupling is in the creation flow's combined job PICKER, not in the data** — easy to misread as proof of coupling and stop. **(2) STOP CONDITION 2 DID FIRE — for home department only, and it is a STORAGE problem, not a missing policy.** `accessible_departments` stores the home department **in the same flat array as granted extras with nothing distinguishing them**, and `composeAccessibleDepartments` always pins the primary first. On a move there are exactly two outcomes and **no way to choose correctly**: keep the array and the **old home becomes standing access nobody granted** (silent broadening, which this tranche's own contract forbids), or rewrite it and **genuine cross-department grants vanish**. `AssociateRosterPanel`/`updateSpecialistScope` already does the former — **a precedent, but not a decided contract**. **RECOMMENDATION: model the home department as its own concept and derive access from it — do NOT add a third column.** **(3) NEW SERVER ROUTE `PATCH /api/roster/members/[id]`** (sibling of the existing super-admin-only `POST /api/roster/members` creation route; Next **16.2.12** `params: Promise<{id:string}>` per `node_modules/next/dist/docs`). **Updates by immutable `id` + actor `store_number` — never `onConflict: "store_number,name"`**, which is exactly the writer that **cannot express a rename** (ROSTER-LIFE-001). **Authority-sensitive keys are REFUSED OUTRIGHT (400), not ignored** — `role`, `accessible_departments`, `pin`/`pin_code`, invite tokens, `auth_user_id`, `username`, `is_active`, `store_number`, and all three department columns — so a caller can never believe an escalation was accepted, and **an escalation smuggled beside a valid edit rejects BOTH halves**. **Returns the re-read row, not the request.** **AUTHORIZATION IS A COPY OF THE GRANT ROUTE'S GATING, NOT A NEW MODEL:** MasterAdmin protected from every actor, `department_supervisor` limited to associates, store scope from the actor and **never** the body. **Creation stays admin-only — unchanged and intentional**; correcting a typo is closer to granting access (which supervisors already do) than to adding a person. **(4) FIRST API-ROUTE TEST IN THE REPOSITORY — and it was cheap, not ambitious.** `vitest.config.ts` **already** aliases `server-only` to an empty build (AI-SAFETY-001 put it there), so testing `PATCH` needed **two `vi.mock` calls plus a small thenable PostgREST stand-in** — no new infrastructure. Real behavioural proof that update filters are `{id, store_number}`, that **`upsert`/`insert`/`delete` are never reached**, and that read-back is genuine — the fake DB **upper-cases the name on write** and the response shows the **database's** value, a stronger claim than any source-text assertion. **(5) CACHE LESSON APPLIED WITHOUT REDISCOVERY:** `updateMemberWorkforceDetails` drops `invalidateRosterCache()` before returning, so the corrected name **cannot revert under its own success toast** (third tranche running — the pattern held). **Nothing reuses `saveSpecialist` or `persistSpecialistFields`.** **(6) DISCOVERABILITY WAS THE SIMPLEST PART** — the user believed no edit feature existed **because none did**. One **`Edit details`** button beside a **`Member details`** heading is the entire affordance; **read-only text was never tappable and still isn't**, and authority actions stay under `Administrative actions` so pairing / PIN / grants / removal never start looking like ordinary profile editing. **Failure keeps the editor open holding what was typed; a duplicate name returns 409; an unreadable read-back returns 500 rather than `ok`. No `offline: true` success path.** **1005 tests / 67 files pass** (from 962/66; +43 in `lib/roster-edit001.member-workforce-edit.test.ts`), typecheck and build pass (`/api/roster/members/[id]` in the manifest), **lint at exact baseline parity — 114 (95 errors, 19 warnings)**. **NO MIGRATION. NO PRODUCTION INSPECTION WAS NEEDED** — ROSTER-LIFE-001 had already confirmed every column exists, so **zero production queries were run and zero rows were touched**. **PARKED:** home-department editing (decision above), secondary operational capability, username-as-display, test-credential guard, roster IA, call-out dialog. **Do NOT mark field accepted.**
- **ROSTER-LIFE-001 — MEMBER EDIT & REMOVAL INTEGRITY — REMOVAL REPAIRED (CODE-ONLY) / EDIT STOPPED PENDING DECISION. NOT COMMITTED — NOT PUSHED.** *Two field defects repaired; the third finding is that the editor does not edit.* **(1) REMOVAL DELAY = ROSTER-ROLE-001 AGAIN, IN A SECOND WRITER.** `deleteSpecialist` mutated local roster state and never dropped the 45s TTL entry, so `reload()` after a successful delete **replayed the removed member back into the list**. Audited all nine mutations in `lib/specialists.ts`: **only `saveSpecialist` invalidated**; `deleteSpecialist`, `setSpecialistActive`, `persistSpecialistFields`, `updateSpecialistScope`, `updateSpecialistPin`, `adminResetSpecialistPin`, `updateSpecialistCredentials`, `resetSpecialistCredentials` did not. Fixed **one line**, placed immediately after `deactivateLocal` so offline / soft-delete / hard-delete paths all benefit. **Two tranches running with the same root cause — treat missing cache invalidation on mutation as a pattern, not a coincidence.** **(2) REMOVE-CONFIRM = FIELD-PROVEN UX-005G SURFACE.** It is **inline JSX inside the always-mounted `RosterTab`** (`{deleteTarget ? ...}`, `fixed inset-0 z-[80]` under the `z-10` KeepAlivePanel), so the mount-gated pattern is impossible — `useFocusedWorkspace(Boolean(deleteTarget))` is the honest open signal. It had **no bottom inset**, so with the nav suppressed Cancel/Remove would sit on the gesture bar; now uses `.hub-modal-sheet` (`p-4` → `px-4 pt-4`). **The sibling call-out dialog is byte-identical in shape and was deliberately LEFT ALONE — no device has reported it. Evidence gates the law, not resemblance.** **(3) THERE IS NO EDIT-MEMBER FEATURE — the user was not failing to find it.** `SpecialistEditSheet` is named like an editor and is not one: name, `floor_title`, and home department render as **read-only text**; it offers schedule, cross-department grants, QR pairing, PIN reset, and Remove. **No form field for name, phone, job title, or department exists anywhere in the roster.** Classification **E6 + E2**, explicitly **not E1**. **(4) I STOPPED BEFORE BUILDING IT — no safe server write path exists.** `saveSpecialist` upserts `onConflict: "store_number,name"`, so a **rename** would miss the existing row, attempt an insert carrying the existing PK, fail, and be **swallowed by a `catch` that returns `offline: true`** — a rename that reports success and writes nothing. `persistSpecialistFields` calls `setActiveSpecialist` on its offline branch, **hijacking the editor's own session identity**. Both write **directly from the browser client**, so **RLS is the only thing between an Edit form and client-side `role` mutation** — building on either would weaken RBAC. **Editing needs a server route (`PATCH /api/roster/members/[id]`) with explicit authority enforcement — a product decision about who may change what, not a repair. Contract proposed in the tranche report; nothing built.** **NO MIGRATION WILL BE NEEDED:** production already has `name`, `phone_number`, `floor_title`, `username`, `assigned_department`, `home_department`, `is_active`, `status`. **Production evidence (read-only, count/schema only):** 21 rows, **all `is_active=true`** (16 active / 5 invited) — **zero soft-deleted rows**, so removal is genuinely hard-deleting when FKs allow, even though the dialog copy says "deactivated" (noted, not changed — wording is a product call). **962 tests / 66 files pass** (from 949/65; new `lib/roster-life001.member-lifecycle.test.ts` + UX-005G contract 27 → **32**), typecheck and build pass, **lint at exact baseline parity — 114 (95 errors, 19 warnings)**, all four flags pre-existing and merely line-shifted. **PARKED:** secondary operational capability, roster IA, call-out dialog, body-scroll debt, Back-to-close. **PRODUCTION MUTATIONS: NONE** (count 21 before and after). **MIGRATIONS: NONE.** **PROCESS HAZARD FOUND:** exporting `.env.local` into the shell made a `vitest` run inherit live credentials and attempt to reach production — no mutation occurred, but **the suite must never inherit real credentials**; worth a guard later. **Do NOT mark field accepted.**
- **ROSTER-ROLE-001 — SECONDARY ROLE PERSISTENCE INTEGRITY — CODE REPAIR COMPLETE / FIELD ACCEPTANCE PENDING. NOT COMMITTED — NOT PUSHED.** *The write was never broken; the read-back was.* **THERE IS NO SECONDARY-ROLE MODEL IN DEPTSYNC.** The only multi-select chip control in the entire component tree is `DepartmentAccessChips` (the sole `role="checkbox"` in `components/`), labelled **"Cross-department access"**, rendered by `SpecialistEditSheet` and `AssociateRosterPanel` — **not** by the Add Team Member sheet, which has only Name / Role `<select>` / Home Department / Phone. **These chips grant DeptSync AUTHORITY SCOPE, not workforce capability** — they drive `canAccessDepartment`, `accessibleDepartments`, the header `AdminDepartmentSwitcher`, `MapTab` allowed scopes, labor availability, profiles sync, JWT `app_metadata.accessible_departments`, and RLS `jwt_matches_department_code`. A supervisor reaching for them to record *"this associate also works Paint"* is recording an **access grant**, and DeptSync has **no home for a secondary operational capability** — flagged for product decision, **no schema proposed**. **PRODUCTION PROVED THE WRITE PATH CORRECT** (read-only, zero mutations): `store_specialists.accessible_departments` exists, is populated across all 21 roster rows, and holds a genuine two-department grant `["plumbing","electrical"]` written **during the very session that reported the failure**; `profiles.accessible_departments` exists too. **The drift hypothesis was wrong and cost one query to disprove** — worth remembering after RUNTIME-COMPAT-001. **ROOT CAUSE = G (write succeeds, read path drops data):** `fetchSpecialists` serves from a **45s TTL cache**, and `updateDepartmentAccess` invalidated `invalidateStoreOpsListCaches()` but **not** `invalidateRosterCache()` — so the `reload()` after the grant replayed the **pre-toggle roster** and overwrote fresh state, reverting the chip under a success toast. **Deterministic, not a race:** inside the TTL window `getSWR` returns the entry and does not even schedule a background refresh. **THE FIX IS ONE LINE** in the mutation that owns invalidation — `updateDepartmentAccess` now calls `invalidateRosterCache()`, matching `inviteSupervisor` / `issueRosterPairing` / `createRosterMember`, **which also repairs `AssociateRosterPanel` without touching it** (composition, not per-component patching). **Two truthfulness repairs of the RUNTIME-COMPAT-001 species came with it:** the grant route's **strip-and-retry** (drop `accessible_departments`, write `assigned_department` alone, return `ok: true`) was **dead compatibility code that could only ever lie** — production has the column, so it now **fails closed**; and the route echoed the **requested** array, which the client stored under the name `persisted` — it now echoes **what the saved row actually holds**. **RBAC/authority semantics untouched:** the grant patch carries departments only (no `role`, no `floor_title`), and the MasterAdmin and "supervisors may only grant on associates" guards are unchanged and contract-tested. **949 tests / 65 files pass** (from 938/64; +11 in `lib/roster-role001.department-access-readback.test.ts`, which **reproduces the stale-cache revert** before proving the repair); typecheck and build pass; **lint at exact baseline parity — 114 (95 errors, 19 warnings)**, all three touched files clean. **PRODUCTION MUTATIONS: NONE. MIGRATIONS CREATED/APPLIED: NONE.** No associate was created or modified for testing. **Do NOT mark field accepted.**
- **UX-005G.1 — ADD TEAM MEMBER FOCUSED WORKSPACE COVERAGE — ENGINEERING COMPLETE — SAMSUNG FIELD ACCEPTANCE PENDING. NOT COMMITTED — NOT PUSHED.** *One field-proven surface. One existing mechanism. One narrow extension.* **UX-005G itself is FIELD ACCEPTED / CLOSED** (real Samsung DS device) — **do not reopen it**; this is a dot-release extension, and **no additional UX-005 identifier was consumed** (UX-005D/E/F untouched). **Field evidence:** with **Roster → Add Team Member** open, the persistent BottomNav remained visible and covered the lower **Cancel** / **Add to roster** controls. **The surface was not missed — it was deliberately deferred.** UX-005G archaeology found it and classified it **D — ambiguous / unchanged** precisely because no device evidence existed; the device has now resolved the classification to **A — focused workspace**. That is the classification discipline working as designed: **the sixteen remaining A-class overlays stay untouched until a device says otherwise.** **Archaeology correction worth carrying:** `AddTeamMemberSheet` is **not its own file** — it is a local, non-exported function inside `components/hub/tabs/RosterTab.tsx` (`:651`), rendered from exactly one call site (`:517`), conditionally mounted on `addOpen` from two `canManage`-gated triggers (header `:404`, footer `:507`). **Exit paths: backdrop tap, Cancel, and successful add** — all funnel through the same `onClose` → `setAddOpen(false)` → unmount. **There is no X icon** (unlike the other five), **no Escape handler**, and no Back integration; the acceptance script must therefore say *X/Cancel*, not *X*. **Pattern A selected** (`useFocusedWorkspace()`, argument-free) because mount **is** the open signal — no `open` prop exists to pass; identical to Edit Bay and the associate editor. **THE FIX IS TWO LINES: one import, one hook call.** No new mechanism, no new state, no shell prop, no `display:none`, no DOM lookup of BottomNav, no custom show-nav callback. The occupancy store and `NavigationHub`'s single consumption point absorbed a sixth surface **without changing at all**. **One placement detail was load-bearing:** `WorkflowTabShell` keeps `RosterTab` mounted for the entire session, so a claim in the **tab body** would have hidden the nav **permanently, everywhere**. The claim sits inside the conditionally-rendered sheet, and a test asserts the hook appears **after** `function AddTeamMemberSheet(` and **exactly once** in the file — this is the kind of error that reads fine in review. **Safe area required no change:** the sheet already carried `pb-[max(1rem,env(safe-area-inset-bottom))]`, identical to the three UX-005G surfaces left alone, so it never relied on nav occlusion; nothing was added and **nav compensation in particular was not**. **No nesting** — `DepartmentPicker` / `TextField` are inline form controls, not overlays; the refcount remains the sole mechanism and was not extended. **Tests extended in place** (`lib/ux005g.focused-workspace-nav.contract.test.ts`, 21 → **27**), not duplicated into a new architecture. **Real render was not available and this is stated plainly:** the component is **not exported**, so isolating it is impossible, and rendering `RosterTab` would require mocking the roster/shift data layer — disproportionate for a two-line wiring change and it would drag data-layer mocks into a navigation test. The **mechanism** already has real-render behavioral coverage (`OnDutyAssociateStrip`, plus refcount/nesting/unmount), so what UX-005G.1 adds is **wiring only**, proven by the accepted wiring-contract idiom. **938 tests / 64 files pass** (from 932/64); typecheck and build pass; **lint at exact baseline parity — 114 (95 errors, 19 warnings)**, the two `RosterTab` flags being pre-existing `set-state-in-effect` at `:159` / `:213`, shifted one line by the import. **Adjacent debt recorded, not fixed:** this sheet locks **no** body scroll at all, unlike the other five — it belongs with the already-deferred body-scroll-lock inconsistency. **Still deferred:** sixteen A-class overlays, Back-to-close, body-scroll locking, UX-005D/E/F. **Explicitly untouched:** Supabase, migrations, roster persistence, authentication, role assignment, QR/device pairing, RBAC, rotations, coverage, verification, barriers, appliances, `tmp/`. **PRODUCTION MUTATIONS: NONE.** **Do NOT mark UX-005G.1 field accepted.**
- **UX-005G — FOCUSED WORKSPACE NAVIGATION SUPPRESSION — FIELD ACCEPTED / CLOSED** (real Samsung DS device; one exception found and discharged by **UX-005G.1** above — Roster → Add Team Member). *Global navigation belongs to navigation state; it does not belong inside a focused operational workspace that already owns its own exit.* **IDENTIFIER:** mandated as UX-005D, reconciled to **UX-005G** — `UX-005D` (*Map operate vs investigate*) and `UX-005E` (*Operational language*) were both already reserved and remain **open and untouched**, as does `UX-005F`. This tranche was **added out of sequence** because Samsung evidence arrived before the D–F questions were answered; **arriving early is not authorization to preempt them.** **Field evidence (real Samsung DS device):** the persistent bottom nav sat on top of five focused surfaces that each already own an explicit X — **Edit Bay, Team Roster associate editor, Department Taxonomies, Associates On Duty, Sunday Rotation Engine** — consuming scarce viewport and physically covering Save and lower controls. **Art. XVI:** persistent application chrome MUST never make operational work unreachable; **treated as one interaction-state defect, not five CSS bugs.** **Why it could not already be suppressed:** `BottomNav` is rendered from **exactly one place** (`NavigationHub`) behind a `showBottomNav` prop that has existed since the component was written and that **no caller has ever passed** — the escape hatch existed, the state to drive it did not; and because `NavigationHub` mounts from **three** route shells (`WorkflowTabShell`, `app/page.tsx`, `app/flooring/page.tsx`) while the five surfaces sit arbitrarily deep inside keep-alive panels, **no prop can travel from a sheet up to the nav.** The defect *looked* like z-index: an active `KeepAlivePanel` is `absolute z-10`, which creates a stacking context, so a `z-[80]` sheet inside it is clamped beneath the root-level `z-30` nav regardless of its z-index — `HubPortal` exists to escape exactly this and four other sheets use it, but **none of the five did.** **Portalling all five was the other available fix and was rejected:** it would make each sheet *cover* the nav rather than establish that global navigation does not belong inside a focused workspace at all — covering is a paint trick, suppression is the product rule. **One owner: `lib/ui/focused-workspace.ts`** — a module-level refcounted store that owns occupancy and nothing else; it never renders and does not know what BottomNav is. Surfaces declare `useFocusedWorkspace(open)`; **`NavigationHub` is the only place that maps occupancy onto chrome.** **Restoration rides effect cleanup**, so X, Cancel, save-and-close, Escape, backdrop tap, route change, error dismissal, and plain unmount all release through the same teardown — **there is no "show the nav again" callback to forget and no path that can leave the nav stuck hidden.** **Occupancy is a COUNT because coexistence is proven, not hypothetical:** `SpecialistEditSheet` opens its pairing dialog at `z-[90]` **while remaining open**, and `AisleBayManager` holds four independent overlay slots nothing prevents from being truthy together — under a boolean, closing the pairing QR would restore the nav beneath a roster sheet that still owned the screen; a counter is three lines more and is the difference between correct and wrong (Art. XX earns its complexity). **THE TOP HEADER IS NOT SUPPRESSED** — store, department, connectivity, and identity stay useful while working, and the sticky header carries no occupancy condition; contract-tested. **Two sheets were relying on the nav as an accidental bottom spacer:** Taxonomies and Sunday Rotation Engine had **no bottom inset at all**, so with the nav gone they would sit on the gesture bar — both now use the existing **`.hub-modal-sheet`** (device safe area only). **No nav-stack padding was added anywhere:** all five surfaces are `fixed inset-0`, so the nav never contributed layout to them and **there is no dead spacer to reclaim** — device safe area and nav compensation stayed separate quantities. **`--hub-workspace-pad-bottom` / `.hub-scan-dock` are untouched** (they compensate tab content *beneath* an overlay and are correct again the moment the nav returns). **NOT extended to the other 26 overlay surfaces** — they are classified A/B/C/D in the tranche report and **left unchanged**; the law is applied to field-proven surfaces, not to everything that is `fixed inset-0`. **This is NOT the `hideChrome` prohibition:** the OIE "no wholesale `hideChrome` restoration" gate concerns `ZebraChecklist`'s unrelated content prop, which was **not touched**. **Prior mobile work preserved and contract-tested:** UX-004C stacking (`active ? "z-10"` intact, `.hub-bottom-nav` rule intact, `HubPortal` → `document.body` intact), UX-004B `replaceState`-preserving-`history.state` semantics (**no history entry is pushed for suppression — Back still exits the page, close-on-Back does not exist and was not invented**), UX-004C.1 Floor Pad intent, TOPO-UX-001, and the Floor · Map · Roster · More IA. **932 tests / 64 files pass** (from 911/63; +21 in `lib/ux005g.focused-workspace-nav.contract.test.ts`, which renders the real `OnDutyAssociateStrip` and the real refcount, including the nested-close case); typecheck and build pass; **lint at exact baseline parity — 114 (95 errors, 19 warnings)**, new files contributing zero. **Explicitly untouched:** Supabase, migrations, rotation logic, coverage logic, verification, barriers, RBAC, appliances, `tmp/`. **PRODUCTION MUTATIONS: NONE.** **Field acceptance ran on a real Samsung DS device and PASSED for these five surfaces** — the single exception was Roster → Add Team Member, which was classified ambiguous here and is now discharged by **UX-005G.1**.
- **RUNTIME-COMPAT-001 — PRODUCTION RUNTIME CONTRACT REPAIR — IMPLEMENTATION COMPLETE — AWAITING REVIEW. NOT COMMITTED — NOT PUSHED.** *Three operator-facing actions were broken against the live database, all in the same way: the code wrote a column production does not have.* **No migration was written or applied; production was inspected READ-ONLY and treated as the authority, and the code was moved to it.** **(1) P0-1 barrier writer.** `insertRotationBarriers` sent `bay_id` / `cycle_number` / `assigned_week` / `reported_by`; production `rotation_exceptions` has `rotation_id` / `department_id` / `location_id` / `reason` / `logged_by`. Every barrier report failed — which is why the table holds **zero rows** (Art. V.1.7: barriers must not silently disappear; they had never arrived). **Production's hand-made shape is the better one:** a barrier belongs to the rotation attempt it interrupted, and week/cycle are reachable through `rotation_id → weekly_rotations`, so the removed columns were denormalized copies, not lost data. The runtime input already carried `rotationId` and `locationId`, so this was a **writer correction, not a schema gap**. Location evidence now also stamps `carried_over` / `last_carried_over_at` alongside `status = CARRIED_OVER` (all three exist), and the per-bay update loop became **one `.in()` statement**, shrinking the partial-failure window from N writes to 1. Ordering is **evidence-first**: exceptions insert before location state, so the surviving partial state keeps the history. **`RotationException` was corrected to production's shape**, which exposed two UI readers (`MapTab` barrier overlay, `SupervisorAuditSummaryModal` rollup) that had been reading `bay_id` and getting `undefined` forever — both wanted the location id. **(2) P0-2 Verify All succeeded then reported failure.** The bay loop verified correctly, then `stampDepartmentWeekVerified` wrote `departments.last_verified_week` / `last_verified_at`, which do not exist, so the DS saw an error over genuinely verified work. **The stamp was DELETED, not replaced** — those fields are derived. `buildVerificationSummary` now derives them from the week's active rotations via the existing **`isRotationVerifiedComplete`** predicate (no second copy of the rule, Art. VII.2); the week is verified only when **every** active rotation is truly `VERIFIED_COMPLETE`, because `is_completed` alone is a report. **THESIS-001A is intact and untouched** — supervisor gate, no-auto-verify-on-replay, and the state guards all still hold. **(3) P0-3 Edit Bay Save** wrote `store_locations.department_code`; no such column exists, `department_id` was already in the same patch, so the invalid write was removed and nothing added. **(4) Two readers were concealing all of it.** `listRotationExceptions` filtered `rotation_exceptions.assigned_week` and treated the missing-column error as *no exceptions this week* — indistinguishable from a clean week; it now resolves the week relationally (`weekly_rotations!inner(assigned_week)`, applied only when a week is requested) and **lets errors surface**. The verification queue read `sunday_bay_assignments.assigned_week` (production has **`week_starting`**, a date) and **discarded the error**, silently losing every associate name; it now uses the existing **`isoWeekToMondayDate`** helper — no third week resolver — plus store scoping, because a bare date is shared by every store. **(5) Three fallbacks removed — each had become the hazard it was written to prevent.** `completeWeeklyRotation` retried without the review columns, and a row with `is_completed` and no `verification_status` **resolves as `VERIFIED_COMPLETE`**, closing the location as `COMPLETED` with nobody having verified it. `updateRotationRow` in `rotation-review.ts` had the **same** strip-and-retry on the verify and send-back paths — **removed as inseparable from proving Verify All correct**, not as opportunistic cleanup. Superseding fell back to a hard **`DELETE`** when `superseded_at` looked absent, destroying the history superseding exists to keep. Production has all these columns, so none was still a compatibility path; each now **fails closed**. Surviving `superseded_at` guards are read-side re-queries and were deliberately left alone. **The new suite models production's real column lists and rejects unknown columns the way PostgREST does**, so the pre-repair payloads fail against it — a permissive fake is exactly what let these ship. **911 tests / 63 files pass** (from 893/62); typecheck and build pass; **lint at exact baseline parity — 114 (95 errors, 19 warnings)**, new suite contributing zero. **Deliberately untouched:** `weekly_rotations.cycle_number`, `departments.current_cycle_number`, any migration, cycle admission/rollover, week-clock unification, coverage engine, RBAC-TOPO-001, schema snapshot regeneration. `tmp/` untouched. **PRODUCTION MUTATIONS: NONE.**
- **THESIS-001 — DISCOVERY COMPLETE** and **THESIS-001A VERIFICATION AUTHORITY REPAIR — IMPLEMENTATION COMPLETE — AWAITING REVIEW.** **NOT COMMITTED — NOT PUSHED.** *An associate may report completion; an associate may not create DS verification truth.* **Two authority leaks closed, both by removing a claim rather than adding a check.** **(1) T-1 — legacy batch verify REMOVED, not re-gated.** `POST /api/rotations/verify` called `requireSupervisorOrAdmin` only inside `if (reviewAction)`, so a request that omitted `review_action` fell through to a legacy path writing `is_completed`, `verification_status = VERIFIED_COMPLETE`, `verified_at`, `verified_by`, and `store_locations.status = COMPLETED` after requiring **only an authenticated actor** — and `isDeptFloorActor` includes `associate`, so the lowest-privilege actor could close the bay it had just reported (Art. VI.2 / XIII / XVIII.3). **Deletion, not a moved role check**, because the branch had **zero runtime callers** (`verifyWeeklyRotationBatch` was called by nothing), barrier reporting already has its own actor-scoped owner in `POST /api/rotations/exceptions`, and `verify_all` already covers bulk close — A1.10 precedent, **obscurity is not a control**. The supervisor gate now wraps the **whole POST method**, so fail-closed is structural rather than per-branch, and the route rejects any request without an explicit `review_action`. **The acceptance law named *direct helper call* as a bypass**, so the `completedRotationIds` loop inside `verifyWeeklyRotations` was also removed; the remaining `departments.last_verified_*` stamp is now `stampDepartmentWeekVerified`, renamed because a function named `verifyWeeklyRotations` that cannot verify anything lies about its authority. `verification.ts` can no longer write `VERIFIED_COMPLETE`, `verified_at`, `verified_by`, `is_completed`, or location `COMPLETED` **at all** — contract-tested against **comment-stripped** source (an assertion a comment can satisfy is not an assertion). **(2) T-5 — offline replay never auto-verifies.** `/api/rotations/complete` derived `autoVerify` from the **live session**, so an associate's offline completions flushing while a DS held the device became DS-verified with no supervisor ever looking at them (Art. XIV.3). **The payload's `specialist_id` / `specialist_role` were deliberately NOT trusted** — Art. XVIII.3 forbids client assertion as authority, and using them to *grant* auto-verify would rebuild the defect with extra steps. The queue persists **no server-verifiable actor provenance**, so the mandate's fail-closed option was taken: **a replayed completion lands as reported-complete awaiting DS review, even when the original actor genuinely was a DS.** One extra tap; a verification record that always means a supervisor decided. **The safety property is an asymmetry: the `replayed_from_queue` marker can only *withhold* authority, never grant it** — role still comes exclusively from the server-resolved actor, so forging the flag de-escalates the forger and omitting it grants nothing the live session lacked. The rule has a named owner, `resolveCompletionAutoVerify`, so it is **unit-tested directly** rather than asserted as route text. **(3) State guards — implemented, bounded.** Verify now refuses a bay nobody reported and is **idempotent** on an already-verified bay (preserving the original verifier's provenance, not overwriting it); send-back refuses both an unreported and an already-verified bay, closing `VERIFIED_COMPLETE → PENDING`. **The guards read the RAW column, deliberately NOT `resolveVerificationStatus`** — that resolver infers `VERIFIED_COMPLETE` from `is_completed` when the column is absent, so on a pre-migration database every associate submit would look already-verified and DS verification would refuse work it should close; an empty stored value means "two-stage review isn't represented here" and the guard stands down. **A guard that infers state can manufacture the transition it exists to prevent.** **Reopening verified work stays unavailable — product decision, not defect** (no UI; it desyncs the attempt-history child). **LIVE SCHEMA WAS NOT CONFIRMED and this is not glossed:** no Supabase CLI, no `config.toml`, no database MCP; a `.env.local` exists but connecting to production with a service-role key is not a code tranche's business. **The repair was therefore built to be correct whether or not `20260818_weekly_rotation_verification.sql` is applied.** Separately: **`supabase/schema.sql` predates verification entirely** — it declares neither the review columns nor `weekly_rotation_completion_attempts` — so the incremental migrations are the declared truth, not that file. **Metric integrity holds:** `isRotationVerifiedComplete` reads the raw column, so `verifiedComplete` is reachable only through the two surviving authorized writers (`rotation-review.ts` DS review, `rotations.ts` live auto-verify). **Residual reported and NOT fixed:** a Master Admin can set `store_locations.status = "COMPLETED"` via topology `PATCH` (a `department_supervisor` may only set `CARRIED_OVER`) — that is cool-down state, **not** `verification_status`, so it cannot forge the verified metric, and it sits inside Art. III.1. **Deliberately untouched: T-2 coverage debt, T-3 `/api/rotations/assign` department scope, T-4 `sunday_bay_assignments.assigned_week`, T-6 destructive restaging defaults, `manual_priority_count` decay, historical coaching read-back.** **893 tests / 62 files pass** (baseline 872/61; +20 from `thesis001a.verification-authority.test.ts`, +1 on the UX-002 contract; the UX-002 and SI-refresh enumerations **amended, not deleted**); typecheck and build pass; **lint at exact baseline parity — 114 (95 errors, 19 warnings)**, the new suite contributing zero. The one pre-existing `prefer-const` in `resolveDepartmentForActor` was **left alone** — byte-identical at baseline and outside mandate. `tmp/` untouched. Record in [`OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md`](docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md) Workstream J.
- **SNAP-DECISION-001 — DECISION: RETIRE — COMPLETE** and **SNAP-RETIRE-001 BAY AUDIT VALIDATE RETIRED — IMPLEMENTATION ACCEPTED — CLOSED.** The program's **second retirement** and fifth implementation tranche. **Do not start AI-SAFETY-002. Do not start FLOORING-AI-001. Do not modify Visual Bay Scan.** *Do not repair a capability whose intended authority violates the product's truth model.* **The durable lesson: a dead capability can still carry architectural risk when a missing connection would activate an authority path the product should not have.** Snap Bay wrote no reachable row and gated no reachable completion, yet it was the most dangerous Gemini surface in the repository, because one missing prop stood between it and a model verdict blocking authoritative work. **Severance is not safety.** **The decision was not made on the severed lifecycle.** It was made because the *intended job* — a Gemini verdict gating `weekly_rotations` completion — conflicts with **Art. X** ("MUST NOT independently determine … completion state as operational truth") and **Appendix B row B**; wiring the missing `rotation_id` would have moved the capability from harmlessly severed to actively unconstitutional. **Repair is a product build wearing a bug's clothing** — the visible defect is one missing prop, but nine of thirteen required repairs were absent, including an object-storage subsystem the repository has never had (`rg` for `storage.from` / `.upload(` returns **zero** hits). Three further grounds: the photo is never retained, so provenance (Art. VIII) could only be *built*, not repaired; **no field-value evidence existed and none could have**, because the row was unreadable and the photo discarded — which is also why **INSUFFICIENT EVIDENCE was unavailable**, since no experiment could decide it without first building the repair (leapfrogging); and physical supervisor verification plus the two-stage review/send-back queue already own the completion decision (Art. VI.2). **All fifteen retirement premises were re-verified at HEAD `4eb6d7b` before any deletion.** **Removed:** `/api/ai/bay-audit/validate` and its now-empty `app/api/ai` tree; `lib/store-ops/ai-bay-audit.ts`; `lib/ai/contracts/bay-audit.ts` and its now-empty directory; `lib/store-ops/bay-audit-logs.ts`; `validateBayAudit`, `BayAuditValidateResult`, `BayCompleteGatedError`, and the audit fields of `CompleteRotationExtras` (including their forwarding through the offline `STORE_OPS_COMPLETE_ROTATION` payload); the modal's persisting branch, `auditContext`, `onAuditValidated`, and verdict→cleanliness remapping; Floor's `bayAudits` / callback / `externalAudits` plumbing **and the Snap Bay Photo action**; `ZebraChecklist`'s `auditByRotation`, `externalAudits`, `gatedRotationId`, gated-error handling, and `handleSupervisorOverrideComplete`; the FAIL gate + 422 gated response + override write in `/api/rotations/complete`; the `bay_audit_logs` join in `rotation-review.ts`; the never-rendered verdict chip and `<img>` in `SupervisorAuditSummaryModal`; the offline-capability entry. **NO SUBSTITUTE WAS INSTALLED and none may be** — not a stricter role check, not a deterministic completion gate, not another AI verdict, not a repaired rotation binding. The product job was rejected. A contract test asserts the completion route contains no `score`/`verdict`/`rubric`/`threshold`/`confidence` in **executable** code (comments stripped via a `readCode` helper — an assertion a comment can satisfy is not an assertion). **Actor-scope exposure closed by deletion, not patching:** the validate route never called `assertActorCanAccessDepartmentId`, so any signed-in actor incl. `associate` could write a row against any department in their store (Art. XIII); completion-route protections are unchanged and contract-tested. **HISTORICAL DATA PRESERVED:** `bay_audit_logs`, its indexes, and its RLS are untouched — **no migration, no dropped table, no deleted or rewritten rows, no normalised verdicts, no fabricated rotation ids or image URLs, no inferred confirmation state, no backfilled provenance.** It is now historical persistence with **no runtime owner**, which is the accepted outcome. **VISUAL BAY SCAN IS PRESERVED AND NOT RE-DISPOSITIONED** — `/api/store-ops/ai-bay-scan`, `buildBayScanPrompt`, `normalizeBayScanResult`, `BAY_SCAN_RESPONSE_SCHEMA`, `resolveImageMimeType`, the shared `GEMINI_TOKEN_BUDGET.bayScan`, and all **three** mounts (Map, `WalkTheFloorSheet`, Cycle Audit) survive; the dependency ran audit → scan and never the reverse, so nothing was stranded; the shared modal is now **ephemeral by contract at every mount**; **its status remains FIELD EVIDENCE NEEDED** and **no mount or feature may be added** — retirement is not authorization to expand it. **Floor entry decision:** the Snap Bay Photo action was **removed**, not converted. Converting it would have handed Visual Bay Scan a fourth mount and decided that it belongs on Floor — a UX-005F product question, not retirement cleanup. The possible ephemeral Floor entry is recorded as a **UX-005F follow-up**. **Field gate: NOT required for the retirement** (every removed downstream behavior was already dead), but the Floor drawer now opens on **five** shift actions instead of six, so **UX-005C's still-pending Samsung acceptance must be run against the five-action drawer** — no new Samsung test was manufactured for dead plumbing. The UX-005C contract test was **amended, not deleted**. **Gemini inventory is now five consumers — four API routes plus one Server Action** (Pre-Flight parse, Floor-Walk Copilot, Visual Bay Scan, Flooring Insights + Executive Floor Pad Server Action); do not conflate the counts. **AI-SAFETY-001 transport is untouched:** one bounded attempt, 20 000 ms, zero retry, SDK confined to `lib/ai/gemini.ts`. **872 tests / 61 files pass** (baseline 843/60; +29 from `lib/store-ops/snap-retirement.test.ts`); typecheck and build pass; build output no longer lists `/api/ai/bay-audit/validate` and still lists `/api/store-ops/ai-bay-scan`; **lint at exact baseline parity — 114 (95 errors, 19 warnings)**, new test file contributes zero. `tmp/` untouched. Full record in [`OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md`](docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md) §5 A1.13.
- **AI-SAFETY-001 BOUNDED GEMINI TRANSPORT — IMPLEMENTATION ACCEPTED — CLOSED:** Reviewed and accepted with **no field gate**; the bounded transport contract is now canonical. The program's first pure **resilience** tranche — **no Gemini capability removed, added, or re-dispositioned, and no consumer file changed.** *An optional intelligence dependency may fail; DeptSync's operational workflow may not fail with it.* **Surviving surface confirmed from code: exactly six consumers** — Pre-Flight parse, Floor-Walk Copilot, Executive Floor Pad extract, Flooring Insights, Visual Bay Scan, Bay Audit Validate — which is **five API routes plus one Server Action** (do not conflate "six consumers" with "six routes"). `callGeminiFlashJson` has exactly six invocation sites and `lib/ai/gemini.ts` is the **only runtime importer** of `@google/generative-ai` (`gemini-schema.ts` is type-only); **no bypass exists.** `callGeminiFlash` now performs **one bounded attempt**: a transport-owned `AbortController`, `GEMINI_REQUEST_TIMEOUT_MS = 20_000`, `signal` passed to `generateContent`, and `clearTimeout` in `finally` on every exit path. **The SDK's own `timeout` option was deliberately NOT used** — it calls `setTimeout(abort)` with no `clearTimeout`, so it would satisfy "has a timeout" while leaking a timer on every success. **Zero retry was added because none existed:** the SDK's `makeRequest` issues exactly one `fetch`, nothing in the app retries a Gemini call, and the offline queue replays a closed enum that contains no AI action — so "no retry ceiling" meant there was **no retry to bound**, and adding one would have raised paid exposure under a safety label. **Do not add Gemini retries, backoff, circuit breakers, retry queues, provider abstraction, or AI telemetry.** Timeout value is **evidence, not taste**: it matches the existing `STORE_OPS_FETCH_TIMEOUT_MS = 20_000`, and a test pins them together so they cannot drift; where the serverless ceiling is shorter the platform still terminates first, and that limitation is recorded rather than hidden. **Abort ownership is timeout-only** — no consumer supplies cancellation, `GeminiCallOptions` exposes no `signal`, and a contract test asserts signal composition **stays uninvented**. **Error contract: one new type only.** `GeminiTimeoutError` (with `timeoutMs`); non-2xx keeps the SDK's `GoogleGenerativeAIFetchError` and its `.status` so quota stays distinguishable from 400; network, malformed-JSON, and not-configured errors are byte-identical. Timeout copy is `"AI request timed out after 20s — check the connection and try again"` — **deliberately checked against `humanizeSupabaseMessage`**, which would otherwise relabel any message containing `does not exist` / `schema cache` / `jwt` / `failed to fetch` / `networkerror` as a migration or credential fault. **Preserved in full:** every prompt, schema, normalizer, token budget, persistence path, confirmation boundary, fallback, and each consumer's own `isGeminiConfigured()` branch. **One behaviour improves as a consequence, not an edit:** `VisualBayScannerModal` sets `phase` in `try`/`catch` with no `finally`, so a hung request previously stranded the "Scanning bay…" overlay permanently with capture disabled; it now rejects at 20 s into the modal's existing `catch` and returns to `capture` — the component is unmodified. **One test-infra change:** `vitest.config.ts` aliases `server-only` to the package's own empty build, because the transport was otherwise unimportable and source-text assertions cannot prove abort, timer cleanup, or non-retry; a contract test asserts `import "server-only"` is still present. Validated **843/843 tests across 60 files (27 new)**, typecheck clean, build clean, **lint byte-identical at 114 problems (95 errors / 19 warnings)**. **No field acceptance required** — the successful path is unchanged (same request, prompts, schemas, parsing, UI, no added latency) and the failure semantics are fully provable with mocked `fetch` + fake timers; no test sleeps for the real timeout. **Adjacent findings recorded, not fixed:** Floor-Walk Copilot can produce **two** concurrent parses from one Stop tap (`finishListening` has no idempotence latch, `runParse` no in-flight guard; `FloorPadEditor` already uses a `finishedRef`) — a **consumer** double-dispatch, not a transport retry; Bay Audit Validate still persists before a client can abandon (**SNAP-DECISION-001**); Floor Pad autosave + unvalidated `metadata` (**AI-SAFETY-002**); `storeOpsFetch` still has no client-side timeout; `ChunkErrorBoundary` still has zero importers (**RESILIENCE-001**); `asGeminiSchema` is still a cast; `.env.example` model drift unchanged. **Not committed. Do not mark CLOSED before review. Do not start AI-SAFETY-002.** Full record in [`OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md`](docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md) §5 A1.11 / A1.12.
- **AI-RETIRE-001 SNAG TRIAGE RETIRED — IMPLEMENTATION ACCEPTED — CLOSED:** The program's **first retirement** rather than reduction, and its third closed implementation tranche. **The route must not be reintroduced behind a stricter authorization gate and no deterministic Snag Triage replacement may be built** — the product job itself was proven unnecessary. **AI-SAFETY-001 still applies in full** to the surviving Gemini paths; removing one unbounded call site here was incidental and does not reduce or close it. `POST /api/push/dispatch` remains a separate Super-Admin-gated orphan candidate with no action authorized. Deleted `app/api/ai/snag/triage/route.ts`, `lib/store-ops/ai-snag-triage.ts` (prompt, `SNAG_TRIAGE_RESPONSE_SCHEMA`, normalizer, `buildLocalSnagTriage`, `capSnagTriageText`, `formatEquipmentNote`), `lib/store-ops/snag-dispatch.ts`, `lib/ai/contracts/snag-triage.ts`, and the never-called `triageSnagReport` / `SnagTriageClientResult` from `lib/store-ops/client.ts`. **Orphan status proved, not assumed** — searched by symbol, route URL, request/response field names, written table names, prompt text, and the `source: "snag_triage"` marker: no caller, no UI, no deep link, no cron, no external integration, and no offline replay (sync queue uses a closed `SyncActionType` enum; SW treats `/api/` as network-only). **Escalation GEMINI-001 understated:** the gate was `requireStoreOpsActor` — *any* signed-in actor including DeptFloor — so `dispatch: true` could write `downstock_queue`, `rotation_exceptions`, and `shift_walk_tasks` at the lowest auth tier. **Deterministic fallback retired with the job** (`buildLocalSnagTriage` was `normalizeSnagTriageResult({}, input)`, zero external consumers); **no deterministic replacement built, nothing wired into Floor / Map / Walk & Talk / Predictive Copilot.** **No tombstone** — the `ai-note-summary` 410 exists to redirect real prior consumers; this job never had one. **Historical rows untouched; no migration, no cleanup SQL.** Preserved: shared Gemini transport, `GEMINI_TOKEN_BUDGET.copilot` (shared with Floor-Walk Copilot + Executive Floor Pad), `reportRotationBarriers`, `createWalkTaskId`, and all five surviving Gemini routes. Validated 816/816 tests (14 new), typecheck clean, build clean (route absent from manifest), lint byte-identical at 114 problems. **No field acceptance required** — no user-reachable surface ever existed. Full record in [`OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md`](docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md) §5 A1.10.
- **AI-REDUCE-002 DETERMINISTIC CATALOG TAXONOMY — IMPLEMENTATION ACCEPTED — CLOSED:** Second closed OIE implementation tranche, and the **first closed without a field gate** — the gate is earned by changed field-facing behavior, not by removing AI. **Adjacent provenance finding (plan §5 A1.9, not a work item):** legacy audit `sub_category` values on devices that used Generate may hold model-authored folder labels; no new ones can be created, and **historical rows are deliberately untouched** because provenance cannot distinguish them safely — no migration, no normalization, no repair UI. Deleted `app/api/catalog/ai-taxonomy/route.ts` and `lib/catalog/ai-taxonomy.ts` (prompt, `TAXONOMY_RESPONSE_SCHEMA`, `normalizeAiTaxonomy`, `buildLocalTaxonomy`, `composeTaxonomyWithDefaults`), the Generate interaction and its loading/error state in `TaxonomyManagerModal`, `saveTaxonomyOverride`, and the `GEMINI_TOKEN_BUDGET.taxonomy` key. `DEFAULT_DEPARTMENT_TAXONOMIES` is now the sole folder-tree producer. **Premise verified in full** — Gemini was prompted with the defaults, never saw catalog or audit evidence, and contributed no unique capability. **Two findings beyond GEMINI-001.** (1) The generated taxonomy is `localStorage`-only, but `DepartmentAuditSection` writes the selected folder into a persisted audit's `sub_category`, so model-invented folder names could reach audit evidence. (2) **A live department-identity defect:** `mergeTaxonomies` resolved identity as `incoming.department_code || base.department_code`, so a blob stored under `D25` carrying `department_code: "D35"` made `getTaxonomyForDepartment("D25")` return a D35 tree. Identity is now pinned from the storage key and restored after merge; a test asserts it. **No user state destroyed** — the override map had exactly one writer (the AI handler) and no editor exists, so stored values are provably machine output; they are left readable and clearable, with no new writer. Validated 802/802 tests (18 new), typecheck clean, build clean, lint byte-identical to baseline at 114 problems. **No field acceptance required under Law 8** — the only changed surface is the Master-Admin-only taxonomy manager in Settings; the associate-facing audit drill-down renders byte-identical. Shared Gemini transport and the six other Gemini paths untouched. Full record in [`OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md`](docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md) §5 A1.8.
- **AI-REDUCE-001 — FIELD ACCEPTED — CLOSED:** Documentation-only closeout; no runtime, test, API, schema, migration, or Gemini change; `tmp/` untouched. **The first fully closed implementation tranche under OIE.** Acceptance was a real DS-device operational test: **12 bays staged**, the workflow exercised as though those bays had been worked, the staged work **cleared/completed**, and DeptSync observed **updating itself to reflect the changed operational state**. That is the whole of the reported evidence — no prose, latency, network, offline, or per-bay observations were reported and none may be inferred. What it validates: **when underlying operational evidence changes, DeptSync recomposes its state from that evidence rather than depending on a Gemini rewrite** — the core AI-REDUCE-001 requirement. The deterministic briefing remained operational after Gemini removal, operational state kept responding to changed evidence, no Gemini dependency was needed for that response, and no new intelligence architecture was required. **This result is capability-specific and does not transfer** to Floor-Walk Copilot, Executive Floor Pad, Flooring Insights, Visual Bay Scan, Bay Audit Validate, or Catalog Taxonomy, and it does not close UX-005F. Full evidence record in [`OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md`](docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md) §5 A1.7. *Less machinery. Same truth. Same usefulness. Lower dependency.*
- **AI-REDUCE-001 DETERMINISTIC SHIFT BRIEFING — IMPLEMENTATION (commit `ded0bdd`):** First runtime tranche under OIE. **Shift Briefing no longer calls Gemini.** Deleted `app/api/store-health/ai-summary/route.ts` and the client's `fetchShiftBriefing`; removed the briefing prompt, `SHIFT_BRIEFING_RESPONSE_SCHEMA`, `normalizeShiftBriefing`, `isShiftBriefingTransportError`, the now-dead `compactTelemetryForPrompt`, and the exclusively-owned `GEMINI_TOKEN_BUDGET.briefing` key. `buildLocalShiftBriefing` is now simply the briefing producer — the "AI result vs local fallback" split is gone, and `ShiftBriefingClient.source` narrowed to `"local" | "session"`. **No operational information was removed and no new intelligence was added:** headline, the three captioned bullets (Focus Bay / Pending Barriers / Quick-win), and priority department all still derive from the same health + bay-health evidence via the same builder. **Refresh semantics changed on evidence:** the repository proved the tap/pull gesture served *only* the AI rewrite in normal operation (`refreshAi` did `snapshot ?? await fetchStoreHealth(...)`, so with a snapshot already loaded it never re-read evidence). Rather than delete a meaningless interaction, refresh now performs the useful deterministic job it appeared to do — `fetchStoreHealth(..., { force: true })` invalidates the SWR entry and re-reads store health. UI copy made truthful: "optional AI rewrite" → "Shift health brief · tap or pull to refresh"; the `· AI refresh` provenance label is gone and `· local metrics` became `· store metrics`. **Preserved:** IndexedDB `shift_briefings` hydration, SWR, offline usefulness, mobile pull gesture, role/access behavior, and the session-refresh auth empty state (now reached deterministically via `isStoreOpsAuthFailure` instead of through the AI endpoint's soft 200). **Untouched Gemini surfaces:** Pre-Flight, Floor-Walk Copilot, Floor Pad, Flooring Insights, Visual Bay Scan, Bay Audit Validate, Catalog Taxonomy, and `callGeminiFlash` itself. AI-SAFETY-001 was **not** implemented here. Validation: **784/784 tests across 57 files** (20 new in `lib/store-ops/shift-briefing.test.ts`), typecheck clean, build clean, **lint identical to the HEAD baseline (114 problems / 95 errors / 19 warnings — pre-existing, unchanged)**. **Engineering complete ≠ field accepted:** a narrow **Samsung / real DS-device field smoke** remains pending under OIE Law 8 because the card is field-facing on the supervisor's own phone and the refresh gesture behavior changed. Not committed.
- **GEMINI-001 GENERATIVE COST & NECESSITY AUDIT — DISCOVERY COMPLETE, DISPOSITIONS RECORDED:** Documentation only. **No runtime code, test, API, schema, migration, or Gemini behavior change; nothing removed; no reduction implemented; `tmp/` untouched.** Canonical dispositions live in [`docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md`](docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md) §5 Workstream A (A1.1–A1.6). **Central finding: Gemini is not the operational intelligence engine** — DeptSync is almost entirely independent of it. Every rotation, attention, health, velocity, staging, and labor producer is deterministic TypeScript with zero Gemini calls; Gemini appears only at input-interpretation and prose boundaries. Scope is **nine live call paths plus one 410 tombstone** — one more than RA-001 named, because `VisualBayScannerModal` has **two** backends (three mounts hit the ephemeral scan; the Floor mount hits a separate validating endpoint that writes to the database). Dispositions: **RETAIN AI** — Pre-Flight location parse. **OPTIONAL AI** — Floor-Walk Copilot, Executive Floor Pad extract, Flooring Insights. **REPLACE DETERMINISTICALLY** — Shift Briefing (deterministic and Gemini paths have identical output shape and **zero Gemini-only fields**), Catalog Taxonomy (static input; the shipped registry already covers every catalog code). **REMOVE JOB** — Snag Triage (full stack, zero invocation path, yet can write three authoritative tables if dispatched). **FIELD EVIDENCE NEEDED** — both Snap Bay capabilities. **RETIRED** — Note Summary. **Snap Bay lifecycle verified SEVERED:** `rotation_id` is structurally guaranteed null, `image_url` is never persisted, supervisor read-back cannot join the row, and the FAIL completion gate cannot fire — so paid inference produces orphaned rows and model accuracy is unevaluable. That is **not** a repair decision: `SNAP-DECISION-001` must choose **repair lifecycle or retire capability** on product value and field evidence. Shared safety finding: **no Gemini path has a timeout, `AbortSignal`, bounded execution time, or retry ceiling** (`AI-SAFETY-001`). Also recorded: `asGeminiSchema` is a TypeScript cast rather than runtime validation, and `.env.example` drifts from the code's default model name (truth/configuration debt, unchanged). Separately, **seven of eleven** audited intelligence candidates already have deterministic engines, so the absence of Gemini reduces nothing. **Next candidate: AI-REDUCE-001 — Deterministic Shift Briefing (NOT STARTED).** All new items — AI-REDUCE-001/002, AI-RETIRE-001, AI-SAFETY-001/002, SNAP-DECISION-001, FLOORING-AI-001 — are **NOT STARTED**. UX-005F is **not** closed; GEMINI-001 supplies repository-level evidence only, and no field evidence was invented. WALK-001/FE-004, APP-CAT-001A, and the UX-005B/UX-005C acceptance states are unchanged.
- **OIE-000 OPERATIONAL INTELLIGENCE EVOLUTION PROGRAM — PROGRAM FOUNDATION COMPLETE:** Documentation only. New canonical program map at [`docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md`](docs/product/OPERATIONAL_INTELLIGENCE_EVOLUTION_PLAN.md). **No runtime code, test, API, schema, migration, or Gemini change; `tmp/` untouched.** Built on the RA-001 read-only repository archaeology whose central finding is that DeptSync's gap is not missing capability — the repeating pattern is *evidence exists → derivation exists → sometimes a recommendation exists → user-facing read-back/action is missing or weak*. Program thesis: **the next unit of product value is connection, not construction.** The plan adopts the Constitution's **Article IX layer numbering verbatim** (0 Authoritative State → 1 Deterministic Derivation → 2 Pattern/Temporal → 3 Forecast → 4 Decision → 5 Explanation → 6 UI) and introduces no competing terminology; informal names are mapped, not renumbered. It records ten program laws (Evidence Before Intelligence · Read Before Build · Deterministic First · AI at Unstructured Boundaries · Explainability · No Cause Inference · History at Decisions · Field Acceptance · **Visibility Is Not Validation** · No Leapfrogging), ten workstreams A–J, a ten-question generative-AI decision standard, seven historical-pattern *candidates* (explicitly not approved features), the five RA-001 field experiments FE-001…FE-005 (all **PENDING** — no results invented), a seven-phase evidence-sensitive sequence, a 16-row work-item table, and a do-not-build register. **Nothing is approved for implementation.** Every listed item is an evidence-backed candidate. Key preserved gates: **WALK-001 stays deferred** (and the UX-005C contract test asserting its non-implementation stays in force); **APP-TEACH-001 is blocked on APP-CAT-001A Samsung ESL revalidation + FE-001**; **REC-001 / LAB-001 must not be wired merely because they exist**; **no wholesale `hideChrome` restoration**; no causal employee scoring, no productivity surveillance, no universal risk score, no fifth nav tab. UX-005 remains an independent active program — GEMINI-001 supplies evidence for canonical **UX-005F** rather than replacing it, and UX-005B/UX-005C Samsung acceptances remain outstanding and unaffected. Three RA-001 findings are recorded as **strong static inference, not runtime-confirmed**, and must be verified before any tranche acts on them: the `bay_audit_logs` lifecycle severance (SNAP-001), the `sunday_bay_assignments.assigned_week` production mismatch, and edge protection on `/api/v1/*`. Next planned discovery: **GEMINI-001 — do not mark started.**

## Latest (2026-09-07)
- **UX-005C FLOOR OPERATIONAL SIMPLICITY — IMPLEMENTED — SAMSUNG ACCEPTANCE PENDING:** Presentation / information architecture only. `ShiftAnalyticsDrawer` is preserved as the container (still collapsed by default; same `#floor-pad` hash + `EXECUTIVE_FLOOR_PAD_OPEN_EVENT` opening mechanism; children still mount only when open). Opening it now leads with six shift actions in order — **Walk & Talk Floor Pad → Flag Downstock → Showroom Quick Touch → Predictive Copilot → Weekly Audit Rollup → Snap Bay Photo** — stacked full-width (the old Snap/Downstock 2-col grid is gone) so phones never depend on a horizontal strip. **Snap Bay is retained but demoted to last** — not deleted, not redesigned, Gemini untouched, override-helper debt deliberately unrepaired (UX-005F / field evidence decides its future). Reports moved behind one nested, collapsed-by-default **Reports & insights** disclosure (`ShiftAnalyticsReportsGroup`, exported from the same drawer file, open state **not** persisted, no new global accordion framework): Audit Velocity → Store Health → Shift Briefing → Exception Feed. Exactly one secondary level: drawer → reports. Duplicate presentation removed: the drawer no longer renders `BayFreshnessGrid` (component + `composeBayFreshness` remain in the repo; Floor's own readiness line and Current Attention strip are untouched), and Store Health no longer repeats **Logged barriers** here because Exception Feed owns that list in this context — one explicit prop `showLoggedBarriers` (default `true`), so **every other Store Health call site is unchanged**. No health calculation, barrier persistence, or Exception Feed semantics changed. **WALK-001 — Shift Walk Task Read-Back remains deferred debt**: Walk & Talk still writes `shift_walk_tasks` via `dispatchShiftWalkTasks` and `fetchShiftWalkTasks` still has no UI consumer; UX-005C improved placement only and is contract-tested not to have grown into read-back. Walk & Talk and Executive Floor Pad were **not** merged or renamed (Question #4 stays open). No schema/migration/API route/Gemini helper/engine/scoring/rotation/verification/offline-queue/RBAC/Map/Roster/More/bottom-nav change — nav remains Floor · Map · Roster · More. **No appliance code touched; APP-CAT-001A Samsung ESL validation remains pending.** Contracts: `lib/ux005c.floor-operational-simplicity.contract.test.ts`. 764 tests / typecheck / build pass; lint at baseline parity (114). Do **not** mark UX-005C field accepted.
- **APP-CAT-001A-FIX-001B MISSING-PARENT SYNC CLASSIFICATION — IMPLEMENTED — SAMSUNG ACCEPTANCE PENDING:** The historical identifier FK quarantine is no longer an "Unknown sync failure" with raw SQL as the message. New `SyncFailureReason` value `blocked_missing_parent`, decided by the constraint name `appliance_catalog_identifiers_item_fkey` (structured `constraint` field when available, else message/details) and corroborated by Postgres `23503` when a code is present, scoped to `upsert_appliance_catalog_identifier` so unrelated FK violations are never misclassified. `SyncQueuePanel` leads with `syncActionBlockedExplanation` — "Item 5709242 is not on DeptSync for this store yet. Add the item, then Retry." — with a truthful generic fallback when identity is absent; raw constraint text remains secondary diagnostics. Ownership unchanged (`sync-queue` classifies, `sync-conflict` words, panel renders); **no** new module and the unowned `catalog-sync-dependency.ts` / `parent_missing` code was **not** restored (contract-tested). Recovery unchanged and now tested: replay never creates a parent, never auto-retries; intentionally ensure the canonical parent → supervisor Retry; same-owner replay idempotent, different-owner still conflicts without stealing, alias `id`/`created_at` provenance preserved. No schema/migration/transport change. Contracts: `lib/appliances/app-cat001a-fix001b.missing-parent.test.ts`. The three Samsung records remain untouched. Do **not** declare APP-CAT-001A field accepted.
- **APP-CAT-001A-FIX-001A CANONICAL PARENT BEFORE IDENTIFIER ALIAS — IMPLEMENTED — SAMSUNG ACCEPTANCE PENDING:** The three quarantined Samsung records **were** inspected in Settings → Device & sync. Their visible error was a foreign-key violation on `appliance_catalog_identifiers_item_fkey`, which is `FOREIGN KEY (store_number, item_number) REFERENCES appliance_catalog (store_number, item_number)` — the alias was rejected because its canonical parent catalog row was absent server-side for that store. Their exact item numbers / identifiers were not captured and are **not** claimed. They remain **preserved** and have **not** been retried or discarded. FIX-001 made that failure visible; FIX-001A makes the legitimate workflow succeed. Link Existing now ensures the one explicitly chosen canonical parent server-side **first** via idempotent `POST /api/appliances/catalog/ensure` (actor/store-bound; existing parent returned untouched with `created: false`; insert-only so a concurrent creator wins; 409 rather than stealing a UPC another item owns; teaches no identifiers; no bulk input), **then** the alias, **then** exactly one physical observation. Failure at any step stops the sequence with a visible error — application-level ordering across two requests, **not** a DB transaction. `ApplianceServerRejectionError` is the shared "server answered and refused" contract (`ApplianceCatalogParentHttpError` / `ApplianceIdentifierHttpError` beneath it); only a no-response failure queues. `offline: false` is a **hint only** — the fail-closed `store_number` re-stamp invalidated it — so ensure always runs. Offline teaching queues the parent **ahead of** the alias, and `flushSyncQueue` now rewrites the store queue in enqueue order (the old `[quarantined, deferred, failed]` grouping could invert that dependency and re-trigger the same FK violation). One item only — **not** bulk APP-CAT-001B promotion, which stays deferred. No schema/migration. Contracts: `lib/appliances/app-cat001a-fix001a.{canonical-parent,observation-gate}.test.ts` + queue-order cases in `lib/sync-queue.test.ts`, all verified to fail against the pre-repair behaviour. Do **not** declare APP-CAT-001A field accepted.
- **APP-CAT-001A-FIX-001 + APP-SYNC-UX-001 FIELD REPAIR — IMPLEMENTED — SAMSUNG ACCEPTANCE PENDING:** Repairs the four defects proved by APP-CAT-001A-FIELD-001 real-hardware showroom use. **Identity model held** — unknown ESL → Link Existing → Lowe's item # → existing catalog item, and `many identifiers → one canonical Lowe's item` remains sound. Fixed: (A) proven unbounded active-audit read/render loop — `AppliancePhysicalAuditPanel` reads callback props through refs so `refresh` is stable, and active-session write-back is guarded by `applianceAuditSessionSignal` (id+status); `ApplianceAuditSection` passes stable callbacks. (C) online identifier HTTP 404/500 no longer misclassified as no-response — new `ApplianceIdentifierHttpError` is the received-a-response contract; HTTP success/409/other 4xx-5xx never queue, only network failure / true offline queue. (D) Quick Add sub-category bounce removed — resolved canonical items enter a `classify` completion mode instead of being demoted to unknown-identifier against their legacy UPC; identity + taught aliases preserved; no prompt replay; exactly one physical observation. (B) attention banner copy is now **"N sync issues need attention"** (never "items") and is activatable → `SYNC_QUEUE_INSPECT_HREF` = `/settings#sync-queue` → existing `SyncQueuePanel`; new `syncActionSubject` shows each queued operation's real-world subject from payload evidence only (no raw JSON, no opaque uuids). The three quarantined Samsung records were later inspected — see APP-CAT-001A-FIX-001A above; nothing was cleared, retried, discarded, or purged. No schema/migration. Contracts: `lib/appliances/app-cat001a-fix001.{audit-loop,identifier-failure,subcategory}.test.ts`, `lib/app-sync-ux001.attention.test.ts`. Deferred debt recorded in DEVELOPMENT_JOURNAL. Do **not** declare APP-CAT-001A field accepted.
- **UX-005B DEPARTMENT-AWARE APPLIANCES ENTRY — IMPLEMENTED — SAMSUNG ACCEPTANCE PENDING:** Floor identity header shows contextual **Appliances** / Physical audit when Supervisor+/Master and `workingDepartment === "appliances"` with section access. Navigates to existing `APPLIANCES_OPERATIONAL_HOME_HREF`. More → Appliances remains secondary. No fifth nav tab; Appliances-only pilot; Flooring unresolved (#6 open); no audit-state fetch on Floor; no dual-shell refactor. Contracts: `lib/ux005b.floor-appliances-entry.contract.test.ts`. Do **not** close Question #1 as field-accepted yet.
- **UX-005 GOVERNING BACKLOG PRESERVED:** Full program constraints live in [`docs/product/UX005_OPERATIONAL_SIMPLICITY_BACKLOG.md`](docs/product/UX005_OPERATIONAL_SIMPLICITY_BACKLOG.md). UX-005A/B are early tranches only — not the whole program. Open Question Its Place items (except dual Start owned by 005A; action bar demoted not deleted; #1 implemented pending field evidence), UX/product/architecture debt, do-not-touch guardrails, and sequence **UX-005C→F** remain active.
- **UX-005A APPLIANCES OPERATIONAL HOME SIMPLIFICATION — IMPLEMENTED — REAL-HARDWARE ACCEPTANCE PENDING:** Presentation/IA only around one physical-audit spine (Start → scan → Review/Close → Reconcile → history). Duplicate Start/Continue CTA removed from section; panel owns primary actions; Ad-hoc / Manage mappings / ActionBar / scan log demoted under **More appliance tools**. Capabilities preserved; no schema; no appliance semantics changed. Contracts: `lib/appliances/ux005a.home.contract.test.ts` + APP-* / UX-NAV green. Do not claim Samsung acceptance.
- **UX-005 OPERATIONAL SIMPLICITY AUDIT — DISCOVERY ONLY:** Whole-product mobile IA audit after real-store phone use. Floor/Map/Roster/More preserved; highest-value first move = Appliances home simplification (UX-005A). Remainder of program tracked in governing backlog.
- **APP-ROT-001 APPLIANCE AUDIT CONSIDERATIONS — COMMITTED AND PUSHED — REAL-HARDWARE SMOKE PENDING:** Deterministic evidence composer (`appliance-audit-consideration-v1`) over CLOSED audits + Option B recon snapshots. Advisory **Consider checking again** on Appliances physical-audit home (near Start). No schema, risk score, fixed cadence, stale threshold, or Floor rotation engine. Catalog ≠ expected inventory. APP-INT / CAT-001B / BULK-AUD deferred. ESL CAT field validation not a blocker.
- **APP-CAT-001A MULTI-IDENTIFIER RESOLUTION — COMMITTED, PUSHED, AND PRODUCTION MIGRATION LIVE — ESL / REAL-HARDWARE VALIDATION PENDING:** Many taught identifiers → one `(store_number, item_number)`; legacy `upc` retained; Link vs Create teach; unambiguous sanitized fallback. Migration `20260907_appliance_catalog_identifiers.sql` applied on `fmeinlwhixngednabhgy`. CAT-001B local promotion deferred.
- **APP-OBS-001 FULFILLMENT DISPOSITION — COMMITTED, PUSHED, AND PRODUCTION MIGRATION LIVE — REAL-HARDWARE VALIDATION PENDING:** Per-unit `fulfillment_disposition` (NULL / STAGED_PICKUP / STAGED_DELIVERY); quiet scan + last-unit sticker actions; soft after CLOSED; recon breakdown; CSV column. Not official availability. Migration `20260907_appliance_scans_fulfillment_disposition.sql` applied on `fmeinlwhixngednabhgy`. Next: APP-CAT-001 multi-identifier (after field validation).
- **APP-AUD-002B PHYSICAL EVIDENCE FREEZE HARDENING — COMMITTED, PUSHED, AND PRODUCTION MIGRATION LIVE — REAL-HARDWARE VALIDATION PENDING:** Option B — explicit `audit_session_id` membership (omit = unbound; no server ACTIVE auto-bind); ad-hoc + Floor/SIMS stay unbound; CLOSED freezes WHAT/WHERE/WHEN/WHICH via API + `20260907_appliance_scans_closed_evidence_freeze.sql` (applied on `fmeinlwhixngednabhgy`); soft classification fields remain mutable; late in-window offline bind (001A) preserved; catalog snapshot deferred. Next after 002 closes: **APP-OBS-001**.
- **APP-AUD-002A AUDIT LIFECYCLE CLARITY — COMMITTED AND PUSHED — REAL-HARDWARE VALIDATION PENDING:** Option A — CLOSED = physical count frozen (not recon complete); derived recon progress; recent 3–5 CLOSED audits + View all; just-closed recon banner.
- **APP-ROADMAP-001 APPLIANCE EVOLUTION ROADMAP — COMMITTED AND PUSHED:** Canonical appliance product direction + tranche order in [`docs/product/APPLIANCE_EVOLUTION_ROADMAP.md`](docs/product/APPLIANCE_EVOLUTION_ROADMAP.md). Next scheduled discovery: **APP-AUD-002**. APP-OBS-001 queued after lifecycle clarity + field validation. No runtime change.
- **APP-QA-001 APPLIANCE SURFACE RELIABILITY — COMMITTED AND PUSHED — REAL-HARDWARE VALIDATION PENDING:** Share/Export CSV repaired (DOM-attached download + share abort/fallback). Gemini Scan Anomaly Detection removed from Appliances home. Deterministic reconciliation retained. APP-OBS-001 blue-sticker still deferred pending field validation.
- **UX-NAV-001 SPECIALTY OPERATIONAL HOME — COMMITTED AND PUSHED:** More → Department Tools; Appliances navigates to audit-aware `/?section=appliances`; Flooring tools grouped; SpecialtyToolsHost retained for contextual/ad-hoc only.
- **APP-FIELD-001B AUDIT EXIT WIRING — PRODUCTION CODE LIVE — REAL-HARDWARE VALIDATION PENDING:** Start/Continue Physical Audit primary; ad-hoc labeled secondary; scanner resolves auditSessionId; Review / Finish wired; unbound prior ledger scans not retro-attached.
- **APP-FIELD-001 APPLIANCE RAPID-FIRE / TEACH / AUDIT EXIT — PRODUCTION LIVE — REAL-HARDWARE REVALIDATION PENDING:** Local-first COUNT; fail-closed catalog `store_number` migration (no fictional 0000); scanner Review / Finish Audit. Local phone catalog preserved; no bulk upload.
- **APP-AUD-001 APPLIANCE PHYSICAL AUDIT & RECONCILIATION CLOSED — PRODUCTION LIVE:** Sessions + Option B recon state + observation-time CLOSED bind + close flush/block + clear preserves audit-bound scans. Migration `20260906_appliance_audit_sessions.sql` applied + verified on `fmeinlwhixngednabhgy`. Pre-migration dump retained under `tmp/production-backups/`. No Lowe's/SIMS/Zebra integration; not immutable OH history; no fabricated legacy sessions.
- **APP-UX-001A CATALOG WRITE-PATH VERIFICATION:** Online application failures (400/401/403/409) no longer fall through to direct Supabase; API-only online path; network-unavailable still queues offline teach. Awaiting commit with APP-UX-001.
- **APP-UX-001 APPLIANCE TEACH & CATALOG RECOVERY — AWAITING COMMIT REVIEW:** Manage appliance mappings sheet (search/edit UPC↔Item↔category↔sub↔description); quiet known-UPC scan; Quick-Add teach for unknowns; UPC conflict fail-visible (no silent remap); no reconciliation/OH/session architecture. Offline teach kept; offline mapping **correction** deferred. Not claiming APP-DISC-001 future inventory architecture.
- **TOPO-UX-001 CONTINUOUS DEPARTMENT MAPPING — READY FOR SAMSUNG ACCEPTANCE:** Manual Bulk Generator stays open after success; department + stable defaults retained; aisle clears; start/end kept as mapping template; upsert-safe `saved` copy uses captured aisle; base velocity seed unchanged; seasonal relevance untouched; session ends on explicit close/unmount; no schema/API/auth change. Deferred **RBAC-TOPO-001** (Supervisor UI vs Master-only bulk write).
- **UX-004C.1 EXECUTIVE FLOOR PAD HANDOFF — FINAL GATE:** Durable `?open=executive-floor-pad`; Floor bridge gated on `/dashboard` pathname (keep-alive mount alone insufficient); bridge mounts after Walk & Talk listeners; sync `replaceState` + `router.replace` after open dispatch; commit pending Samsung acceptance.
- **UX-004C MOBILE INTERACTION RELIABILITY:** Samsung PASS except Executive Floor Pad → UX-004C.1. Baseline `0b939be`.
- **TEMPORAL COPY CLARIFICATION LIVE — DEVICE VALIDATED:** Floor **Staging week** / Map **Calendar week**; clocks unchanged; Sunday W37 vs W36 intentional. Commit `8e106f3`.
- **UX-004B DEVICE RE-SMOKE PASSED — UX-004 CLOSED ON REAL HARDWARE:** Quiet + elevated Show all clear on Samsung SM-S948U production; keep-alive path OK.
- **UX-004B QUIET INVESTIGATION CLEAR RELIABILITY FIX IMPLEMENTED:** UX004A-01 — quiet Show all did not reliably clear URL/strip (elevated cleared). Root cause: soft `router.replace` alone failed to sync keep-alive MapTab search params on null store-map page. Fix: History `replaceState(history.state, "", bare)` then `router.replace` (still replace; preserves history.state; no push/reload). Clear invariant is SI-result-independent. Pin policy (UX004A-02) deferred; week-label ambiguity addressed by temporal copy clarification (clocks unchanged).
- **UX-004 FLOOR → MAP INVESTIGATION CONTEXT IMPLEMENTED:** Floor Current Attention → Map preserves why the DS navigated (`investigate=current-attention&dept=`). Floor passes intent only; Map resolves current SI itself (no stale Floor evidence/location list). Geography preserved with marker emphasis; Standard on investigation arrival; Show all clears; MEDIUM/HIGH elevated set unchanged; no ranking/recommendation/Map redesign/SI semantic change/LAB/REC/schema/API. Feature freeze remains outside UI program. UX-004 closed on real hardware after UX-004B.
- **UX-003 FLOOR DECISION HIERARCHY IMPLEMENTED:** Floor remains primary DS command surface; bottom nav unchanged. Final order: **identity → supervisory obligation (pending verification) → week state/freshness → active work** (thin Stage/On-duty + filters + checklist) → **advisory Current Attention** (quiet AVAILABLE demoted) → **fiscal/season context** → **collapsed More tools**. Renamed bay filter **Needs Attention → Open issues** (predicate unchanged). Week telemetry does not precede verification. No LAB/REC runtime, no Map handoff, no backend/SI/rotation semantics. Feature freeze remains outside UI program. Commit/push ≠ production mobile validation.
- **UX-002 VERIFICATION AUTHORITY SAFETY CORRECTION IMPLEMENTED:** UX-001 P0 — Floor Shift Analytics “Verify awaiting review” called `verifyAllCompletedBays` with empty `completed_rotation_ids`, stamping `departments.last_verified_*` while success copy claimed bays verified. **Fix A:** removed duplicate false CTA + `signOffCompleted` / related state from `FloorTab`; removed unused `verifyAllCompletedBays` client helper; false success copy removed. Canonical path sole bay-review owner: Floor verify strip → `SupervisorAuditSummaryModal` → `verifyPendingBay` / `verifyAllPendingBays` / `sendBackPendingBay` (`review_action`). Empty-ID `verifyWeeklyRotations` retained as legitimate department/week stamp after true `verify_all` only (no backend semantic change). Direct legacy API empty-ID hardening deferred. No Floor redesign, schema, migration, LAB/REC. Feature freeze remains. Contract tests: `verification-authority.contract.test.ts`. Commit/push packaging ≠ authenticated production lifecycle proof.
- **REC-001 STAGING CONSIDERATION FOUNDATION IMPLEMENTED:** Pure domain `lib/store-ops/staging-consideration.ts` (`department-staging-consideration-v1`) + 46 unit tests. Answers: while below configured weekly staging target, which unstaged actionable MEDIUM/HIGH Current Attention locations qualify for DS consideration? **Full qualifying candidate pool** (never truncated to `staging_deficit`). `staging_deficit` is **planning context only** — not capacity, not truncation bound, not completion promise. Material SI location-id conflicts (when deficit > 0) → `UNAVAILABLE` (conflict ≠ empty success); deficit 0 short-circuits without SI. No rank/score/ordinal/selected; no LAB; no draw/assignment/verification/target mutation; rotation fairness preserved. No API/UI/schema/persistence/runtime consumer. Not LIVE / not DEPLOYED / not production-verified. **Intelligence foundation phase complete** (SI-001 LIVE; SI-002 deferred; CAP-001 deferred; LAB-001 + REC-001 foundations implemented).
- **LAB-001 DEPARTMENT LABOR AVAILABILITY FOUNDATION IMPLEMENTED:** Pure domain `lib/store-ops/labor-availability.ts` (`department-scheduled-labor-v1`) + 53 unit tests. Scheduled-labor **evidence** only (persisted rows; known vs unknown durations; conflicting duplicates → `CONFLICTING_SHIFT_DAY`; unavailable sources → `null` not zero; gross `known_scheduled_hours` includes call-out; `known_callout_hours` = absent subset). No API/UI/schema/persistence/runtime consumer. No bay capacity, productivity, utilization, recommendation, SI-001, weekly target, or rotation coupling. Default-8 planning heuristics untouched. Day-scoped; home-dept attribution; Supervisors as schedule evidence; Master excluded. CAP-001 remains deferred. Not LIVE / not production-verified (no surface).
- **CAP-001 DEPARTMENT OPERATIONAL CAPACITY — NOT IMPLEMENTED:** Architecture-audited; no code. Repository does not support truthful **inferred bay capacity**. Rejected: people×3 bays; scheduled hours÷productivity constant; `weekly_bay_target` as capacity; individual productivity ratings; inferred bay absorption. **`weekly_bay_target` remains desired staging volume / operational target.** Shift hours remain valid for **relative** Sunday assignment distribution; they do **not** prove bay capacity. No second declared Planning Allowance. Bay-unit capacity may be revisited only when recommendation architecture proves real need. Labor Availability ≠ Capacity ≠ Productivity ≠ Recommendation ≠ Weekly Target. No CAP evaluator/API/UI/schema; no rotation/target mutation; does not revive SI-002.
- **SI-002 OPERATIONAL PRIORITY — DEFERRED / NOT IMPLEMENTED:** Explored via architecture audit (no code). Name/concept rejected for implementation (“Operational Priority” semantically overloaded; command-language risk; collides with draw `priority_*`). Constraint-aware consideration remains architecturally possible but does not earn Day-1 value beyond SI-001 under Arts XX/XXI. **Deliberate product/architecture decision: DEFER** — not a technical failure. No evaluator, API, UI, schema, score, ranking, persistence, or rotation coupling. **Current Attention remains the final current-state intelligence layer until recommendation architecture needs a stronger intermediate boundary.**
- **SI-001C FLOOR ATTENTION SUMMARY LIVE — CLOSED:** Implementation `21e1a72` on production lineage `88da2e81cfd14e841947f012dd1b1aaa63887ea9` (manual Vercel confirmation). Floor compact **Current attention** strip after readiness/fiscal context. Pure tier counts over same `GET /api/store-intelligence/attention` as Map (`location-attention-summary.ts`). MEDIUM/HIGH foreground; NONE/LOW quiet; no department score/rank/top-list/filter/heatmap. Floor owns independent fetch (keep-alive → bounded duplicate GET with Map accepted). Master `all` → select department. Degraded keeps counts + “Some evidence unavailable”; failure → unavailable (not quiet zeros). Device-local `As of` via `generated_at`. Optional **View on Map** → investigation intent URL (UX-004). Staging/Sunday + shift-status do **not** independently refetch SI; successful verify-batch/barrier mutations notify `STORE_OPS_LOCATIONS_CHANGED` so attention consumers refresh. No schema/API/SI-001/A/B semantic changes.
- **SI-001B MAP ATTENTION SURFACE LIVE:** Map shows MEDIUM/HIGH compact Focus+Med/High markers; NONE/LOW quiet; Walk sheet Current attention detail (confidence/actionability/reasons/derived/`As of` **device-local** display of response `generated_at`); seasonal copy uses SI `effect` (CONTEXT vs MODIFY); Master `all` → select department; failure-independent; no sort/filter/heatmap/ranking/rotation coupling. On production lineage `88da2e8`.
- **SI-001A ATTENTION READ API FOUNDATION IMPLEMENTED:** `GET /api/store-intelligence/attention?department_id=` + `location-attention-read-model.ts`. Supervisor+; actor store only; one department; hybrid degradation; empty success ≠ unavailable; barrier openness needs rotation only when exception rows exist; no rank_key; aisle/bay order; no UI/schema/persistence/rotation writes. Not Map/Floor consumption.
- **SI-001 FOUNDATION IMPLEMENTED (deterministic engine LIVE in app build):** Pure `composeLocationAttentionPressure` (`location-attention-pressure-v1`) + shared `location-eligibility.ts`. Pressure NONE|LOW|MEDIUM|HIGH (CRITICAL deferred); actionability orthogonal; confidence = evidence maturity with availability flags; seasonal LOW/MEDIUM/HIGH differentiated; no UI/schema/persistence/rotation wiring. Not Map/Floor/rotation intelligence live.
- **FS-003B Map context surface LIVE:** Map shows explicit active location seasonal relevance (batched `mode=resolve-locations`); cell badge `Seasonal HIGH[+N]`; Walk sheet detail with provenance. UNSET omitted; NONE detail-only; no dept→bay inheritance; no heatmap color overload; no rotation/SI/schema. Production empty contexts → Map visually unchanged until Master declares.
- **FS-003 location seasonal relevance foundation LIVE:** `operational_context_location_relevance` — Master-declared (context × location) relevance; missing = UNSET; CASCADE on context/location delete. No rotation/priority mutation, no Map UI, no SI. APIs: list includes `location_relevance`; `mode=resolve-locations`; Master `PUT .../location-relevance`. Settings card assigns bay exceptions.
- **FS-002B Floor context surface LIVE:** Floor composes fiscal time + active season/event + current department relevance via `FloorOperationalContextStrip` (Supervisor+). Client reads `GET /api/fiscal-calendar` + `GET /api/operational-contexts?mode=resolve`. Empty context valid (fiscal alone). No schema/seeds/rotation effect/SI/location priority. Detail edit remains More→Settings (Master).
- **FS-002 foundation LIVE:** `operational_contexts` + `operational_context_department_relevance` — Master-declared seasons/events (Gregorian ranges), department relevance (UNSET vs NONE), provenance. No company-event seed, recurrence, location priority, or seasonal intelligence. APIs: `GET /api/operational-contexts`, Master CRUD under `/api/admin/operational-contexts*`. Settings: `OperationalContextCard`.
- **FS-001A minimum LIVE:** Derived fiscal coverage (`computeFiscalCoverage`) + Master-only `GET /api/admin/fiscal-calendar/coverage` + More→Settings `FiscalCoverageCard`. No discovery, no promote API, no persistence, no cron/push. FY2027 still not loaded — coverage signal only.
- **FY2026 authoritative fiscal calendar SEEDED:** Lowe's Vendor Gateway `Fiscal_calendar.pdf` page **2026** → `fiscal_years=1`, `fiscal_weeks=52` (`2026-01-31`→`2027-01-29`, Sat–Fri weeks). Provenance: `COMPANY_PUBLISHED` + URL. Holidays/events **not** imported. ISO `assigned_week` remains distinct. Artifact: `data/fiscal-calendars/lowes-fy2026-company-published.json`.
- **FS-001 Fiscal calendar LIVE (schema + app):** `fiscal_years` / `fiscal_weeks` **production applied**. App: `fiscal-calendar.ts` + `GET /api/fiscal-calendar`. ISO `assigned_week` unchanged (fiscal is contextual only).
- **Completion-attempt history LIVE (schema + app):** Table `weekly_rotation_completion_attempts` **production applied**. App wires complete/verify/send-back/auto-verify + history API. Missing-relation skip is table-specific; auto-verify retry recovers from parent stamps; RESTRICT retained. **First natural lifecycle pending** (rotations/attempts currently 0 — do not fabricate). Pre-migration dump `…T17-41-43-893Z.dump` retained.
- **Specialty M2 PRODUCTION VERIFIED:** `20260905_specialty_catalog_remnants_parity.sql` applied live. Hub `carpet_catalog` / `carpet_remnants` recreated (0 rows); PostgREST former 400s → **200 []**. M1 `home_department` already live. Pre-M2 dump `…T15-09-55-089Z.dump` retained with prior dumps. App deploy not required for schema fix. Safe to commit migration + docs when ready.
- **M2 detection strengthened** then applied after gate. Mixed-state fail-closed; full unique `(store_number, sku)` + remnants PK proven.
- **Weekly rotation history (Force Draw):** Incomplete Force Draw / Admin reset **supersedes** `weekly_rotations` (`superseded_at`) instead of hard-delete. Active plan = `superseded_at IS NULL`. Migration `20260905_weekly_rotations_superseded.sql` — **production applied** with app `eb4152d`. Layer-1 metrics still `weekly-rotation-metrics-v1` on active rows only. Assignment-history wipe on restage remains open debt.
- **Layer-1 metrics (A-1):** Canonical owner `lib/store-ops/rotation-metrics.ts` (`weekly-rotation-metrics-v1`). Floor / readiness / store health / weekly rollup / Map week overlay distinguish **reported** vs **verified** (`VERIFIED_COMPLETE` only). No schema changes. Tests: `rotation-metrics.test.ts`. Screenshots/fixtures: `tmp/layer1-metrics-validation/` (do not commit).
- **Constitution:** `DEPTSYNC_CONSTITUTION.md` established (docs-only). Governing laws for operating boundary, human/data/intelligence authority, rotation/verification, UI hierarchy, anti-drift. Baseline `d6d5806`. No app behavior changes.
- **Information hierarchy:** Floor operate-only (Topology demoted to More); verification strip → existing rollup modal; empty week Stage plan; Map navigator before Snap Bay Photo. Screenshots: `tmp/hierarchy-implementation/` (do not commit).
- **Mobile shell scroll:** `.hub-app-shell` = fixed `h-dvh` viewport; keep-alive tabs (`absolute inset-0 overflow-y-auto`) are the scroll owners; `--hub-workspace-pad-bottom` clears the fixed bottom nav. Screenshots: `tmp/mobile-shell-validation/` (do not commit).
- **Two-DS pilot polish:** store identity adopts profile store when hub store unset (`lib/store.ts` + `SessionGate`); Floor readiness line (`composeFloorReadinessLine`); Sunday staged-work / hours→share UX (engine unchanged); remnant live area via `composeRemnantArea`. Ops checklist: `DEPT_SYNC_STATE.md` Appendix D. Screenshots: `tmp/ds-pilot-polish/` (do not commit).

## Product
DeptSync Hub — department-scoped inventory & SIMS audit platform for Lowe's store teams. Multi-category flooring + appliances, barcode scan-to-catalog, dual roll/carton audit engine, SIMS location finder, PWA offline shell + sync queue, multi-store isolation, department-scoped RBAC, specialist PIN / password, CLF/sqft variance, remnant aging, manager markdown, and **Store Operations** (aisle/bay map + automated weekly maintenance rotations).

## Branding
- App: **DeptSync Hub** · PWA short_name **DeptSync**
- Manifest name: `DeptSync — Department & SIMS Audit Hub`
- Layout title: `DeptSync Hub · Department & SIMS Audit` · appleWebApp title `DeptSync`
- Header: brand `DeptSync · #2587` (compact; marquee only if overflow) · section title · **department dropdown pill** · account/PIN
- **Typography:** Geist (`--font-geist-sans` → `font-sans`) + Geist Mono (`--font-geist-mono` → `font-mono`). Bay tags (`formatBayTag`, e.g. `A14-B06`), SKUs, cadence badges, and timestamps use `font-mono tracking-tight`.
- Header badge: `DeptSyncBadge` (floating layers + barcode; boot/PWA uses `branded` cyan/gold so theme FOUC cannot flash emerald)
- Icons: `public/icons/icon-192.png`, `icon-512.png`, `apple-touch-icon.png`, `mark.svg` (cyan/gold floating mark, no enclosed shield)
- PWA manifest: `app/manifest.ts` → `/manifest.webmanifest`; static `public/manifest.json` → `/manifest.json` (TWA / Bubblewrap)
- **Native shell:** haptics via `utils/haptics.ts` + `HapticsListener`; offline toast `OfflineNetworkBanner` + `ConflictResolutionModal`; sync auto-flush on online/visibility/focus; PWA/TWA splash theme `#090d16`; `app/loading.tsx` + `DeptSyncSplash` (pinned midnight + branded mark)
- **Theme engine:** `lib/theme.ts` owns presets + prefs (`deptsync_theme_prefs`). `data-theme` on `<html>`: `midnight` (Cyber-Dark, default) · `cobalt` (Midnight Sapphire) · `emerald` (Industrial Emerald) · `solar` (Solar Daylight) · legacy `amber` / `obsidian`. Toggles: `data-contrast=high`, `data-density=compact`, plus `soundEnabled` / `hapticsEnabled`. Personal UI: header + Settings **Appearance & Theme** (`Palette` → `UserPreferencesDrawer`). Store config stays in Settings. CSS tokens in `app/globals.css`; glass utilities / nav / primary buttons bind to `--accent`, `--background`, `--border`, `--glow-accent`.
- **Audio & haptics:** `lib/ui/feedback.ts` (Web Audio + `navigator.vibrate`). Scan chimes compose via `lib/scan-feedback.ts`; tap pulses via `HapticsListener`.
- **Obsidian-glass UI:** utilities in `app/globals.css` (`.glass-card`, `.glass-panel`, `.theme-accent-surface`, `.theme-nav-active`, `.theme-modal`, `.btn-primary-glow`, `.btn-quick-touch`, `.chip-filter`, `.hub-main`, `.hub-scan-dock`). Canonical Lucide SVG set (`HubIcon` / `NavIcon` stroke 2; variance / aging / bay status pills stroke 1.75 via `StatusPills`).
- **Handheld chrome:** header `pt-safe` + compact `min-h-12` inside shrink-0 chrome; workflow tabs live in a fixed `h-dvh` `.hub-app-shell` — Master/DS: Floor · Map · Roster · Settings; CSA/Specialist: **My Shift** + **Store Map** only. Each keep-alive tab panel (`absolute inset-0 overflow-y-auto`) is the primary vertical scroll owner. Scan Log bars use `ScanActionDock` (`.hub-scan-dock`, `max-w-lg`, stacked on `--hub-bottom-nav-stack` with `env(safe-area-inset-bottom)`). Specialty hub content is `max-w-lg` + `.hub-scan-dock-pad` inside the same viewport shell. Undo toast uses `.hub-toast-dock` so it never covers Log. Sonner stays top-center under the header. No hamburger, More sheet, or Admin Tools drawer. Store Ops pages use `.hub-main` + `--hub-workspace-pad-bottom` so bays / badges / timers clear the fixed dock.
- **Keep-alive tabs:** Floor / Map / Roster / Settings mount in `WorkflowTabShell` (`app/(workflow)/layout.tsx`) so switches are visibility/opacity only (0ms remount). Departments / map / roster use 45s in-memory SWR plus IndexedDB L2 (`lib/store-ops/cache.ts`) so Map/Floor paint the last **successful live** `store_locations` / `weekly_rotations` fetch in <20ms, then revalidate. The header department pin (`useWorkingDepartment`) does **not** wipe IndexedDB; Floor/Map reset React lists on pin change, then peek the target department key. Failed network reads no longer render as an empty week. **Bulk add bays** awaits IndexedDB+TTL clear (`invalidateStoreOpsListCaches`) and dispatches `deptsync:store-locations-changed` so keep-alive Map/Floor replace an empty snapshot immediately. Realtime channels are refcounted per logical name.- **IRP velocity heatmap:** Map toggle `[ Standard Map | Velocity Heatmap ]` plus Snap Bay. Visual Grid is walk-only (`StoreLocationGrid`: aisle accordions with complete/stale counts, Lucide status glyphs, bay tags, Sell/Top, **Pending** chip when rotation status is `PENDING`, tap → `WalkTheFloorSheet`). Bay CRUD lives in Settings **Store Topology & Bay Setup** (`AisleBayManager`: Add Single Bay, Bulk Generator with velocity seed, batch delete, duplicate prune, `EditBayDrawer` hotspot/lock/decay). `GET /api/store-locations` never filters by status; PENDING means available for Sunday draw. Department pin `flooring`/`D23` resolves to the live `departments.id` family before querying locations. Apply `20260814_bay_velocity_heatmap.sql` + `20260815_custom_decay_days.sql` + **`20260817_rls_security_lockdown.sql`** (authenticated store-scoped SELECT on `store_locations`; open anon reads removed).
- **Multi-department access:** `accessible_departments` on roster + profiles. Header switcher when more than one granted dept. Supervisors grant extras from Roster **SpecialistEditSheet** chips. Apply `20260814_multi_department_access.sql`.
- **Daily shift board / call-out:** Roster groups by home department; department accordions start **collapsed**. Compact `SpecialistCard` rows (name, role badge, `07:00 – 15:30`, On-Duty switch, manage). `lib/store-ops/shift-status.ts` writes `associate_shift_days` first (localStorage is cache only). Manage sheet hosts Edit Schedule → `AssociateScheduleModal` (presets Open/Mid/Close, per-day times) plus access chips. Call-out dialog composes `lib/store-ops/call-out.ts` → Sunday pool / proportional redistribute / carry-over loop. Apply `20260815_associate_shift_days.sql` + `20260815_carry_over_priority.sql`. Supervisors + Master toggle duty; Master-only add/delete team.
- **Predictive Shift Copilot:** Floor `ShiftAnalyticsDrawer` (`PredictiveCopilotBanner`). Local patterns from `bay_service_logs` / Sunday assignments / downstock / locations — not Gemini. 1-tap Stage to Shift (`POST /api/rotations/assign`, Supervisor+) or Add to Downstock.
- **Tactical Voice Hub:** Walk & Talk lives in Floor `ShiftAnalyticsDrawer` (`TacticalVoiceFloorPad`). Web Speech + scratchpad → `POST /api/copilot/parse-walk` → dispatch via `lib/store-ops/shift-tasks.ts`. Bay freshness chip `BayFreshnessGrid` composes `lib/heatmap/bay-tracker.ts` from live `store_locations` (not only this week's rotations). `#floor-pad` expands the drawer. Apply `20260815_shift_walk_tasks.sql`.
- **Roster invite:** apply `20260815_unified_auth_token.sql` + `20260815_roster_app_access.sql` + `20260815_roster_auth_link.sql` + `20260815_roster_insert_rls.sql` + **`20260816_roster_floor_title.sql`**. Add Team Member is roster-only (name/role/home department; optional phone for contact / PIN-reset; no Auth user required). Device pairing is **Pair Device via QR** on `SpecialistEditSheet` → `POST /api/roster/pair` (signed 10-minute token, SHA-256 nonce on `invite_token_hash` / `invite_token_expires_at`). Associate scans `/pair?t=` → `POST /api/auth/redeem-invite` (preview, then PIN + confirm) which burns the hash, mints Hub JWT with `store_number`, and lands on Floor. Signup still claims the existing card (`store_specialists.auth_user_id`) instead of duplicating. Cards show Roster Only / Invited / Active plus Specialist / CSA / Supervisor. Forgot PIN still sends a 30-minute SMS link. Apply **`20260817_rls_security_lockdown.sql`** so Hub inventory, roster, manager notes, and Store Ops reads require authenticated `jwt_matches_store` (anon/open SELECT removed). Login does not SELECT the roster as anon — `POST /api/auth/hub-bridge` returns a sanitized specialist.

## AI (`lib/ai/gemini.ts`)
- Server-only Gemini Flash client (`@google/generative-ai` + `server-only`)
- Env: `GEMINI_API_KEY`, `GEMINI_MODEL` (default `gemini-3.5-flash`) — never `NEXT_PUBLIC_`
- Exports: `callGeminiFlash(prompt, options?)`, `callGeminiFlashJson`, `jsonGenerationConfig`, `GEMINI_TOKEN_BUDGET`, `extractGeminiJsonText` / `parseGeminiJson`, `isGeminiConfigured`, **`GEMINI_REQUEST_TIMEOUT_MS`**, **`GeminiTimeoutError`**
- **Bounded transport (AI-SAFETY-001):** one attempt per invocation — transport-owned `AbortController` + `GEMINI_REQUEST_TIMEOUT_MS = 20_000` (pinned to `STORE_OPS_FETCH_TIMEOUT_MS`), `signal` passed to `generateContent`, `clearTimeout` in `finally`. **Zero automatic retry** (none existed; do not add). Expiry throws `GeminiTimeoutError`; every other failure propagates unchanged, so non-2xx keeps `GoogleGenerativeAIFetchError.status`. The SDK's own `timeout` option is unused — it never clears its timer. Callers keep their own `isGeminiConfigured()` branch, fallback, and persistence. Tests: `lib/ai/gemini.transport.test.ts`
- Callers pass `responseSchema` (owned by domain modules) + per-route `maxOutputTokens`: briefing 256 · Snap Bay 512 · Floor Pad / insights 2048 · Pre-Flight parse 2048
- JSON mime is always on; regex fence extraction remains a safety net
- Does not recommend or own institutional knowledge — callers compose prompts and schemas
- **AI Pre-Flight (Bulk Generator):** `POST /api/store-locations/ai-parse` + `lib/store-ops/ai-parse.ts` normalize to `{ locations, corrections_made }`; input capped at 24k chars; UI tab confirms via existing bulk upsert
- **Flooring AI Insights:** `POST /api/flooring/ai-insights` (Store Ops JWT) — server-fetches remnants/audits, runs aging/variance locally, sends a compact findings packet to Gemini (compact-then-narrate). `FlooringAIInsightBanner` does not POST tables.
- **Zebra Shift Briefing:** **Deterministic only (AI-REDUCE-001).** `buildLocalShiftBriefing` composes `GET /api/store-health` evidence; there is no AI endpoint. Tap/pull refresh forces a fresh health read (`fetchStoreHealth(..., { force: true })`), it does not re-word the brief. Auth failure renders the session-refresh empty state.
- **Audit Velocity Chart:** `lib/store-ops/telemetry.ts` + `StoreHealthChart` inside Floor `ShiftAnalyticsDrawer` (06:00–22:00 curve vs linear target; Overall / D23 / D35 pills)
- **Appliance Anomaly Detection:** **REMOVED (APP-QA-001)** — former `POST /api/appliances/ai-anomaly` + Gemini narration. Deterministic reconciliation remains on the Appliances physical-audit home. Future evidence-derived pattern intelligence may return without LLMs.
- **Catalog Taxonomies:** **Deterministic only (AI-REDUCE-002).** `lib/catalog/taxonomies.ts` registry + Settings `TaxonomyManagerModal` (Master Admin); there is no AI endpoint and no Generate action. Legacy `deptsync_catalog_taxonomies` local overrides are still read and clearable but nothing writes them; department identity always comes from application state, never from stored data
- **AI Visual Bay Scan:** `POST /api/store-ops/ai-bay-scan` + `lib/store-ops/ai-bay-scan.ts` + `VisualBayScannerModal` — 720p JPEG q=0.70 / 960px; route cap ~1.5MB; `responseSchema` + 512 output tokens
- **Executive Floor Pad:** Full TipTap notes still owned by `extractTasksAndTag` (`lib/store-ops/ai-note-extract.ts`). Walk-of-consciousness parse is a separate owner: `POST /api/copilot/parse-walk` + `lib/store-ops/ai-walk-parse.ts` (structured location/category/priority JSON, 8k cap, 2048 tokens). `POST /api/store-ops/ai-note-summary` remains **410 Gone**.

## RBAC (`lib/rbac.ts` + `lib/specialists.ts`)
| Role | Scope | Tabs |
|------|-------|------|
| 👑 Master Admin (`MASTER_ADMIN`) | `assigned_department: all` | Floor · Map · Roster · Settings (DB tools, store settings) |
| 🛡️ Department Supervisor (`DEPARTMENT_SUPERVISOR`) | primary + `accessible_departments[]` | Floor · Map · Roster · Settings; department roster + schedules; Settings topology for bay priorities |
| 👤 CSA / Specialist (`ASSOCIATE_CSA`) | primary + granted extras | **My Shift** `/dashboard` · **Store Map** `/admin/store-map` only (Roster + Settings hidden) |

### Developer sandbox (Master Admin only)
- 3 taps on the DeptSync logo within 800ms opens `DevSandboxDrawer` (`HubHeader` + `NavigationHub`)
- Preview As Role: Master Admin | DS Supervisor | CSA Specialist; Simulate Department overlays chrome without changing JWT
- **Danger zone / testing actions:** department + ISO week selectors (defaults to current staging week), two-step **Clear staged rotation** → `POST /api/admin/rotations/reset` (deletes `weekly_rotations` + `sunday_bay_assignments`, resets bays to PENDING, invalidates rotation cache)
- State: `sessionStorage` `deptsync_dev_sandbox` + `deptsync:dev-sandbox` event (`lib/dev-sandbox.ts`). `composeViewSpecialist` keeps real `id` / name / store
- Banner: `⚡ Simulating: [Role · Dept] — Tap to Exit` (`DevSandboxBanner`)
- 3-tap stays wired to the **real** Master (`sandboxActor`) while previewing CSA

### Master Admin roster console
- Canonical team UI is the **Roster** tab (`/roster`, `RosterTab`) — dynamic collapsible accordions for every home department with members (`D23 · Flooring`, `D35 · Appliances`, `D28 · Inside Garden`, …) plus `{count} roster · {onDutyCount} on-duty`. Compact `SpecialistCard` rows; tap / `SlidersHorizontal` opens `SpecialistEditSheet`. Fetch is `store_specialists` by store aliases only (`2587`/`02587`); roster-only rows (`invite_token` / `auth_user_id` null) stay visible. `appliances` / `D35` / `D35 · Appliances` group together. **Add Team Member** → `AddTeamMemberSheet` → `POST /api/roster/members` (roster-only). Pairing is `POST /api/roster/pair` from the manage sheet (QR, no SMS). `SpecialistModal` is a session picker only.
- `/admin/supervisors` and `/admin/roles` redirect to `/roster`
- Invite/reset SMS for **Forgot PIN** still lives on `/auth/verify/[token]`. Roster add-member is roster-only; device pairing is QR (`/pair?t=`). Dead `AdminRosterManager` was removed
- Lightweight **Associate Roster** (`AssociateRosterPanel`) remains on the Sunday assignment drawer
- Floor titles: `store_specialists.floor_title` owns Specialist vs CSA vs Cashier vs Receiving. Specialty job options (Flooring CSA, Appliances Specialist, …) pin home department (`flooring` / D23 family). Cards badge Specialist / CSA / Supervisor. Apply `20260816_roster_floor_title.sql`.
- Sunday Shift Balancer allocates weekly bay quotas to on-duty Specialists/CSAs by 4h / 6h / 8h (`planProportionalBayAssignments`)

### Settings tools (Master Admin / Supervisor)
- Four cards in `SettingsSection` (hosted by `SettingsTab`): Profile & Preferences · Department Targets & Sunday Auto-Stage · Store Topology & Bay Setup · Catalog & Remnants
- Master: store number (Profile accordion), Sunday auto-stage time + auto-run (`SundayScheduleCard`), Store Topology (`#bulk-generate`), Taxonomies (`#taxonomies`), Force Rotation (`#weekly-rotation`)
- Supervisor + Master: weekly targets matrix + Store Topology. Floor Pad lives in Floor `ShiftAnalyticsDrawer` (`TacticalVoiceFloorPad`); Settings `#manager-notes` redirects to `/dashboard#floor-pad`
- Remnant inventory (`#remnants`) when RBAC allows; Device & sync for every Settings role
- Personal **Appearance & Theme** (all roles, including CSA via header) — not store configuration
- Department Supervisors never see Master-only setup controls
- Master Admin header: **My Department Context** pin (All / D23 Flooring / D35 Appliances / …) — persists across Floor · Map · Roster · Settings and filters those tabs in place. Does not navigate to specialty `/?section=` scans.

### Sunday Flooring Cycle Audit
- Staging card + assignment modal: open weekly Flooring bays → assign from on-duty roster; Auto-Assign All to Me; Stage/Draw 12 or **Recalculate** (Master Admin); **Shift balancer** (hours / start–end → proportional clustered zones)
- Associate roster in the drawer **pre-selects Flooring / D23** home-department associates (`associateMatchesSundayDepartment`). Appliances / Millwork / Cabinets start off. Master Admin can toggle anyone. Header: `Selected: N Flooring associates (… from other depts unselected)`. Job-title badges (`Flooring CSA`, `Appliance Specialist`)
- Card shows on Sunday even before bays are drawn (`shouldShowSundayStaging`). Tap opens the drawer **in-place** (`requestSundayAuditDrawer`). `/flooring`, `/sunday-audit`, and `/sunday-rotation` redirect to `/dashboard` with the drawer — they do not 404
- Assignments persist in `sunday_bay_assignments` (JWT store/dept RLS); `bay_id` = `weekly_rotations.id`; ISO week → `week_starting` Monday
- Plan math: `lib/store-ops/weekly-rotations.ts` (does not generate rotations or persist)
- Entry points: Floor tab, Cycle Audit scan, `/flooring` deep link, `#sunday-audit`
- ZebraChecklist live-handoff: `SUNDAY_AUDIT_EVENT` + Realtime via `lib/store-ops/realtime.ts` (bind `postgres_changes` before `subscribe()`, unique channel names, unsubscribe on unmount)

### Departments
`flooring` (D23) · `appliances` (D35) · `plumbing` · `electrical` · `lawn_garden` · `paint` (D24P) · `millwork` (D30) · `cabinets` (D29) · `building_materials` · `hardware` · `tools` (D25) · `all`

- Department glyphs: Lucide `DepartmentIcon` (no emoji). Roster / Admin pin / Settings department matrix / department pickers.
- Store Map Department Overview was removed; Cabinets (D29) weekly target 6 and cron toggle live in Settings `DepartmentTargetsMatrix`.
- Department seed upserts with `ignoreDuplicates` against live UNIQUE: `(store_id, code)` when present, else `code` (`departments_code_key`). Duplicate D29 is logged, not a 500. List falls back to unscoped `SELECT *` so Store Map hydrates existing rows instead of a red banner.

- Seeds: none auto-injected. Create Master / Supervisor profiles via Add Team Member (roster-only); pair devices with **Pair Device via QR** (`status=invited` until `/pair` sets a hashed PIN)
- First-login: non-dismissible AuthWall setup when `must_change_credentials` (no Remind Later)

## Authentication (Zero-Access Wall)
- Unauthenticated visitors never see workspace tabs/data — edge `proxy.ts` (Next 16 middleware) redirects to `/login` (`AccessGate` + AuthWall). `public/robots.txt` disallows all crawlers; `X-Robots-Tag: noindex, nofollow, noarchive` is set globally.
- Login / unlock: username + password/PIN → `POST /api/auth/hub-bridge` (service-role PIN verify). Roster **list** is authenticated + store-scoped and never selects `pin_code` / hashes. Pre-login AccessGate does not fetch `store_specialists` as anon.
- **HTTP-only hub gate:** after PIN + Hub-bridge JWT, `POST /api/auth/gate` sets `deptsync_hub_gate` (8h, HttpOnly). Middleware will not render `/dashboard` without it. Logout clears the cookie.
- **Hub PIN → Auth bridge (primary Store Ops unlock):** after PIN verify, client calls `POST /api/auth/hub-bridge` which service-role verifies the roster PIN, ensures `auth.users` + `profiles` link, and mints a Supabase Auth session. **Master PIN** requires `HUB_MASTER_PIN` (no default). It authenticates an **existing** Master Admin only — it does **not** auto-bootstrap. Recovery: `POST /api/auth/bootstrap-admin` (Bearer `BOOTSTRAP_SECRET` or `CRON_SECRET`) or `scripts/bootstrap-admin.mjs`.
- **Bootstrap recovery:** `POST /api/auth/bootstrap-admin` (Bearer `CRON_SECRET`) or `node --env-file=.env.local scripts/bootstrap-admin.mjs` — resets `master_admin` roster + Auth + profiles
- **PIN reset:** `POST /api/auth/reset-pin` (Change PIN, Super Admin) hashes `pin_hash`. Self-service: AuthWall **Forgot Access Code** → `POST /api/auth/pin-reset/request` → SMS `/auth/verify/[token]` (30m, consume-on-entry)
- **Store Ops identity:** `resolveStoreOpsActor` loads `profiles` where `id = auth.users.id` from Bearer/cookie JWT (no `x-store-ops-*` trust headers; emergency unlock removed). API data paths use service role **after** actor resolve.
- **Returning session:** hub `deptsync_auth_session` alone is not enough — cold restore without a live Supabase Auth JWT forces the PIN unlock wall so bridge can mint Auth.
- **Phone recovery (optional):** "Forgot Access Code? Reset via Phone" → SMS one-time `/auth/verify/[token]` (legacy OTP confirm remains at `/api/auth/phone-reset/*`)
- Setup requires verified mobile (`phone_number` on `store_specialists`)
- Native keychain: form `autocomplete` username / current-password
- Biometric: WebAuthn; requires an existing Store Ops Auth session (otherwise PIN once after fingerprint)
- `must_change_credentials` → non-dismissible permanent credential setup
- Session: `deptsync_auth_session` (hub UI) + `deptsync_hub_gate` HttpOnly cookie (edge gate) + Supabase Auth localStorage (API Bearer); 8h idle lock on hub session
- **P0 boot:** `/login` does not fetch the roster until Hub-bridge JWT exists; `/` and `/dashboard` require the hub gate cookie. Catalog / remnants / appliance catalog load after unlock when the relevant section mounts; specialty scans + Snap Bay / SIMS / Audit Report / Settings tools are `next/dynamic`
- **P0 indexes:** re-run `supabase/migrations/20260813_p0_query_indexes.sql` — hub tables use `store_number`; Store Ops locations/rotations use `store_id`; manager_notes Phase 2 uses `store_number`+`department` (legacy `store_id`+`department_code`). Script skips absent columns. Apply `20260815_performance_indexes.sql` for Map/copilot composites (`idx_store_locations_dept_aisle`, `idx_bay_service_logs_bay_time`, `idx_rotations_active`) on canonical columns (`aisle`/`bay`, `location_id`/`created_at`, `is_completed`).
- **P1 Gemini/map:** Snap Bay 720p + compressed JPEG; Floor Pad Copilot strips HTML / 8k cap; `GET /api/store-locations` explicit Store Map columns (no `SELECT *`); Slice 1 `responseSchema` + per-route token budgets
- **P2 hub UI:** `startTransition` + keep-alive Floor/Map/Roster/Settings (`hidden`); Cycle/Appliance scan forms isolated from logs; 300ms debounced draft saves with flush on submit/leave; weekly rotations + Sunday assignments TTL-cached 45s
- **Settings tools:** Four Lucide cards in `SettingsSection`; Bulk / Taxonomies / Force Rotation stay `next/dynamic`; Sunday schedule is Master-only inside Department Targets; Floor Pad is the Floor tactical dock; SW cache `deptsync-shell-v6-stealth`
- **Bulk bays:** Odd Only / Even Only (`lib/store-ops/bay-pattern.ts`, default odd); Store Map GET falls back if `last_completed_at` is missing/null
- Seeds: no hardcoded roster injection — use Add Team Member (roster-only) then Pair Device via QR (`status=invited` until `/pair` PIN setup)
- Primary: fixed bottom workflow tabs — **Floor · Map · Roster · Settings** only
- Header: DeptSync brand + store # · section title · department dropdown pill · account/PIN chip
- Cycle Audit / Appliances: hardware-scan ready without soft keyboard; Log docked in `ScanActionDock` above BottomNav (`max-w-lg`, safe-area stack)

## Store Ops auth transport
- Client: `storeOpsAuthHeadersAsync` → `Authorization: Bearer` from Hub-bridge or phone Auth session
- Server: `getRequestAuthUser` (Bearer or cookie) → `resolveStoreOpsActor` → `profiles` → service-role DB client
- Soft-fail reads (`auth_required` + hint) only when JWT missing; after Hub PIN unlock banners should clear
- Push subscribe: `user_id` = Auth profile id; `specialist_id` null
- SQL: `supabase/migrations/20260812_jwt_rls_policies.sql` + **`20260817_rls_security_lockdown.sql`** — JWT store/department RLS; anon and `USING (true)` reads removed. Realtime publication includes `sunday_bay_assignments`, `manager_notes`, `downstock_queue`.
## Scan-to-Catalog
- SKU / UPC resolve via `lib/barcode.ts` → `carpet_catalog`
- Roll logging: `RollMeasurementPad` (live CLF/SQYD header + inch/fraction/rounds keypad) composed by `CycleAuditScanForm`
- Soft keyboard: **tap-to-type only** (no auto-focus on tab switch)
- Hardware wedges: `useGlobalBarcodeScanner` (window keydown, 6+ chars ≤150ms) → **active visible** section lookup (`scannerEnabled=false` while hub pane is hidden)
- Mid-scan drafts: `lib/debounced-persist.ts` (300ms) + `useFlushOnLeave`; flooring `lib/storage.ts`, appliances `lib/appliance-scans.ts`
- Focused SKU fields still support Enter **or** rapid burst via NumberField
- Quick-Add modal for unlinked barcodes
- Catalog folders (`lib/catalog-folders.ts`); domain-filtered for department supervisors
- Department taxonomies (`lib/catalog/taxonomies.ts`) for generic dept folder drill-down; registry-only since AI-REDUCE-002

## Dual audit engine
- Mode A (Carpet / Sheet Vinyl): CLF; Mode B: cartons × sqft/box
- Appliances: unit count + SIMS staging; Model # on catalog `vendor`

## Offline & PWA
- Service worker `public/sw.js`; sync queue `carpet_hub_sync_queue` (`lib/sync-queue.ts`)
- Queue actions carry `transaction_id`, `optimistic_at`, retry backoff (`next_retry_at`), optional `base_updated_at`
- Store Ops floor writes (`STORE_OPS_COMPLETE_ROTATION`, `STORE_OPS_DOWNSTOCK_ADD`, `STORE_OPS_SUNDAY_ASSIGN`) use `enqueueOrExecute` — live first, queue on offline / timeout. Queue replay builds a synthetic specialist via `specialistFromSyncPayload` (`is_active: true`). `weekly-rotations.ts` still only plans; persist is `sunday-audit.ts`.
- IndexedDB SWR (`lib/store-ops/cache.ts`) owns durable `store_locations` / `weekly_rotations` / `shift_briefings`; `ttl-cache.ts` remains L1. Writes call `invalidateStoreOpsListCaches()` which clears both.
- `installSyncAutoFlush` — flush on `online`, `visibilitychange` (visible), and `focus`
- Version/409 conflicts → `ConflictResolutionModal` (Keep Local force-overwrite vs Accept Server)
- Header: Online / Offline Mode (`HeaderNetworkStatus`) + amber `SyncStatusPill` when pending > 0

## Multi-store
- `lib/store.ts` (default `1234`); **store switch = Master Admin only** (Settings)
- Store Operations: `stores` table UUID `store_id`; hub session `store_number` maps via `lib/store-ops/stores.ts`
- Departments / locations / weekly rotations scoped by `store_id`; cron runs per active store

## Store Operations upsert keys
- `stores`: `onConflict: store_number` (payload: store_number, name, is_active)
- `departments`: `onConflict: code` (matches live UNIQUE on `code`)
- `store_locations`: `onConflict: department_id,aisle,bay,type` (BOTH writes Selling + Topstock per bay; status PENDING). **`aisle` is alphanumeric TEXT** (`BW`, `RW`, `12`, `A1`) — apply `20260811_alphanumeric_aisle.sql`. Bulk Generator + batch CSV (`lib/store-ops/aisle.ts`) normalize with `.trim().toUpperCase()`; no numeric-only aisle validation.
- `weekly_rotations`: `onConflict: location_id,assigned_week`

## Remnants / markdown
- Aging badges (Lucide, not emoji); 60+ or elevated role → Apply Manager Markdown
- Floor-ops rack bands (Fresh <14d / Watch 14–30d / Critical >30d) via `lib/aging.ts` `classifyRackAging` + `lib/remnants.ts` `remnantRackAlert`; critical rolls show a Suggest markdown chip (markdown math stays in `lib/markdown.ts`)

## Audit Report Export
- Shift summary → **📊 Export / Print Report** (Flooring, Appliances, Department)
- `AuditReportModal` + `lib/audit-report.ts`: formal SIMS/cycle summary, print-to-PDF, mailto / `navigator.share`, Markdown clipboard paste
- Print stylesheet strips hub chrome for letter B&W output

## Store Operations (multi-dept maintenance)
- Schema migration: `supabase/migrations/20260809_store_operations_rbac.sql`
  - `departments`, `profiles` (auth.users + `super_admin` / `department_supervisor`), `store_locations` (SELLING/TOPSTOCK + cycle status), `weekly_rotations`
  - RLS: super_admin all; supervisors read/update own `assigned_department_id`
- Hub bridge: Master Admin → super_admin; Supervisor → department_supervisor (via `departments.code` = hub `assigned_department`)
- **Navigation Hub** (`lib/nav-hub.ts` + `HubHeader.tsx` + `BottomNav.tsx` + `NavigationHub.tsx`): header is `DeptSync · #2587` · section title · department pill · account/PIN; primary tabs **Floor · Map · Roster · Settings** for every role (no More overflow)
  - Floor (`/dashboard`) · Map (`/admin/store-map`) · Roster (`/roster`) · Settings (`/settings`)
  - Authenticated `/` without specialty `?section=` replaces to `/dashboard`. Hub `/?section=audit|appliances|department` is scan tools only. Remnants deep-link to `/settings#remnants`
- `/manager-notes` redirects to `/dashboard#floor-pad` (Tactical Voice Hub on Floor)
- `/dashboard` — one Floor layout for all roles: **Floor Rotation** when Full Store is pinned, otherwise `${activeDept.name} Rotation`; Snap Bay AI Audit + Flag Downstock sheet; on-duty strip filtered to the working department (`canAccessDepartment`); Full Store with >6 on-duty associates collapses to a Users summary sheet. Sunday staging (non-associates), proportional `ZebraChecklist` grouped by on-duty specialists. `store_locations.workflow_type` routes Floor execution: `STANDARD_MERCH` / `BULK_PALLET_AUDIT` = Quick Touch row; `APPLIANCE_SIMS_AUDIT` = `ApplianceSimsChecklist` (scanner stays on Floor with bay `location_id`). Shift velocity, store health, Walk & Talk, briefing, copilot, freshness, showroom, verify/rollup, and `ExceptionFeed` nest in collapsed `ShiftAnalyticsDrawer`. Specialty scan pills remain on the Floor header when the pin allows; Cycle / Appliances also stay on `/?section=`.
- `/stock` redirects to `/dashboard` (Downstock is a Zebra tab on Floor)
- `/roster` — unified team list, PIN add, and department access chips (optimistic + toast)
- `/admin/store-map` — Visual navigator chunks aisle/bay DOM (16 aisles / 24 bays) with memoized Lucide status glyphs + SVG heat strips; Sell/Top is display-only. Tap bay → **one** `WalkTheFloorSheet`. Bay CRUD is Settings **Store Topology & Bay Setup**.
- Sunday staging card opens the assignment modal with **Shift balancer** (hours → proportional clustered zones). Plan owner: `lib/store-ops/weekly-rotations.ts`; persist: `sunday-audit.ts`. Drawer seed: Flooring/D23 only (`associateMatchesSundayDepartment`); Master may toggle other depts.
- `GET /api/store-health` — weekly pace + bottleneck aggregation + compact `bay_health` for DS / Super Admin. Completion % is `computeDepartmentCompletionPct` in `health.ts` (rollup composes the same helper). Bay flag penalties (28/18/16/12) live in `health.ts`; `bay-health.ts` and `weekly-rotations.ts` import `flagPenalty`.
- `POST /api/store-ops/ai-bay-scan` — multimodal bay photo → carton/pallet estimates, cleanliness score, detected issues (Store Ops actor)
- `POST /api/store-ops/ai-note-summary` — **410 Gone**; Floor Pad Copilot `extractTasksAndTag` is canonical
- APIs under `/api/rotations/*`, `/api/store-locations*`, `/api/departments`, `/api/weekly-rotations`
- **Enterprise ingest (stubs, no UI):** Zod contracts in `src/types/enterpriseIntegration.ts` (`BayTopologyIngestSchema`, `FreightStageEventSchema`, `FloorTouchTelemetrySchema`). `POST /api/v1/topology/ingest` and `POST /api/v1/freight/stage` validate with `.safeParse()` and return 400 `{ success: false, error: "Bad Request", issues }` on mismatch. Does not persist into `store_locations` / rotations. Edge gate still requires hub cookie or `Authorization: Bearer`.
- Multi-store: apply `20260809_multi_store.sql`; store scope comes from JWT `app_metadata.store_number` / `profiles.store_number`
- Sunday auto-stage clock: apply `20260816_sunday_rotation_schedule.sql`
- Manager notes: apply `20260811_manager_notes.sql` + `20260812_manager_notes.sql` + `20260812_manager_notes_archive.sql` + `20260812_fix_manager_notes_rls.sql` + **`20260812_manager_notes_metadata.sql`** (`metadata` JSONB from Gemini Copilot)
- Sunday bay assignments: apply `20260812_sunday_bay_assignments.sql`
- Downstock queue: apply `20260814_downstock_queue.sql` (localStorage fallback until applied)
- Shift walk tasks: apply `20260815_shift_walk_tasks.sql` (Floor Copilot dispatch; localStorage fallback until applied)
- IRP velocity heatmap: apply `20260814_bay_velocity_heatmap.sql` (`store_locations.last_serviced_at` / `velocity_tier` / `priority_override` + `bay_service_logs`)
- JWT claims / RLS helpers: apply `20260812_jwt_rls_policies.sql` + enable Custom Access Token Hook + **`20260817_rls_security_lockdown.sql`** (drop anon/open SELECT; Realtime on sunday_bay_assignments / manager_notes / downstock_queue)
- Requires `SUPABASE_SERVICE_ROLE_KEY` for server routes (apply migration in Supabase SQL editor)

## Web Push (rotation phone alerts)
- Migration: `supabase/migrations/20260809_push_notifications.sql`
- Supervisors enable via Settings → Phone rotation alerts (`usePushNotifications`)
- Env: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, optional `VAPID_SUBJECT`
- Generate keys: `npx web-push generate-vapid-keys`
- `POST /api/rotations/generate` dispatches push to matching `department_code` / profile subscribers. Batch body: `{ department_ids, bay_count, force_overwrite }` → `{ success_count, failed_count, staged_bays }` (Settings **Trigger Weekly Rotation** multi-select).
- SW shows notification and opens `/dashboard` on click

## Weekly rotation cron
- Migration: `supabase/migrations/20260809_weekly_rotation_cron.sql` (`weekly_bay_target`, Lowe's codes) + **`20260816_sunday_rotation_schedule.sql`** (`stores.sunday_auto_generate`, `sunday_auto_stage_time` default 05:00, `timezone` default America/Denver)
- `vercel.json`: Sunday `0 11 * * 0` (11:00 UTC ≈ 05:00 AM America/Denver) → `/api/cron/weekly-rotation`. Hobby forbids sub-daily cron (`*/15` fails the deploy). Dispatch still skips unless auto-generate is on, it is Sunday in `stores.timezone`, local time is at/after `sunday_auto_stage_time`, and the week is not already staged
- On Sunday the runner stages the **upcoming** ISO week (Monday). It **skips** a department that already has **active** `weekly_rotations` for that week — Master Admin Force Draw / Recalculate (`force: true`) **supersedes** incomplete active rows via `resetStagedWeekRotations` (preserves `id`/`created_at`), then inserts fresh active bays (insert-after-clear + conflict supersede recovery). Completed (`is_completed=true`) rows stay on the active plan.
- Persist owner: `lib/store-ops/rotations.ts` (`upsertWeeklyRotations`). Payload includes `store_id` (UUID from `20260809_multi_store.sql`), `store_number` (JWT RLS from `20260812_jwt_rls_policies.sql`), and `week_number` + `year` parsed from `assigned_week` via `parseIsoWeekLabel` (`2026-W34` → week 34, year 2026 — never a hardcoded week). If PostgREST reports a missing column (PGRST204 / schema cache), that column is dropped and the upsert retries. A NOT NULL after a cache miss means Postgres has the column but the API cache is stale — reload schema (`NOTIFY pgrst, 'reload schema'`) rather than guessing the column set
- Upsert `onConflict` is **`location_id,assigned_week`** — the table unique from `20260809_store_operations_rbac.sql`, re-asserted by **`20260817_weekly_rotations_location_week_unique.sql`**. Apply **`20260818_drop_weekly_rotations_store_dept_week_uniq.sql`** if the live DB has a mistaken `UNIQUE(store_number, department_id, week_number)`. Admin reset: `POST /api/admin/rotations/reset`
- Env on Vercel: `CRON_SECRET` (Bearer token Vercel sends automatically)
- Settings → Sunday rotation schedule (Master) + weekly bay target matrix (Supervisor + Master; blur autosave + Save All)

## End-of-week verification
- Migration: `supabase/migrations/20260809_rotation_verification.sql`
- Floor **Weekly audit rollup** / amber **Awaiting your verification** strip is the DS verification queue (PENDING_VERIFICATION bays). Apply **`20260818_weekly_rotation_verification.sql`**. Associate complete sets `PENDING_VERIFICATION` (location stays ASSIGNED); DS Verify & Pass → `VERIFIED_COMPLETE` + location COMPLETED. DS/Master complete auto-verifies. Send Back with Note returns the week item to PENDING with `review_note`. APIs: `GET|POST /api/rotations/verify` (`review_action`: verify | send_back | verify_all)
- **UX-002:** Removed false Shift Analytics “Verify awaiting review” CTA that stamped `departments.last_verified_*` via empty-ID batch while claiming bay verification. Department week stamp remains only after true `verify_all` bay review (or explicit ID batch). Client helper `verifyAllCompletedBays` removed.
- `/verify-rotation` and `/admin/exceptions` redirect to `/dashboard`; the Floor `ExceptionFeed` is the live barrier list
- Mid-week floor barriers: `POST /api/rotations/exceptions` (does **not** stamp `last_verified_week`) — Zebra row **Barrier** → tap reason
- APIs: `POST /api/rotations/verify`, `GET|POST /api/rotations/exceptions`

## Selling vs Topstock audit mode
- Canonical Store Ops type `SELLING` | `TOPSTOCK` (`lib/store-ops/audit-location-mode.ts`); hub audits still persist `sales_floor` / `top_stock`
- Cycle Audit / Department Audit / Zebra filter share `AuditLocationModeToggle` — SELLING = lower floor, TOPSTOCK = overheads/racking
- Discrepancy flags, log rows, and audit reports include the mode; Cycle/Department forms keep the mode across logs (not reset)

## Bay workflow profiles
- Owner: `store_locations.workflow_type` (`lib/store-ops/types.ts`). Apply **`20260818_store_location_workflow_type.sql`** + **`20260818_appliance_scans_bay_location.sql`**.
- Settings Bulk Generator + Edit Bay tag `STANDARD_MERCH` | `APPLIANCE_SIMS_AUDIT` | `BULK_PALLET_AUDIT`. Appliances/D35 generate as SIMS. Super Admin **Apply … to all mapped bays** is `PATCH /api/store-locations` `{ apply_to_department, department_id, workflow_type }`.
- Floor routes SIMS bays to a 4-step placard/scan checklist; Complete still hits `POST /api/rotations/complete`. Scanner context is `requestApplianceScanner({ location_id, aisle, bay })` — `lib/appliance-scans.ts` remains scan owner. Recon composes catalog + scans only (`lib/appliances/sims-reconciliation.ts`).

## Store number (dynamic)
- Owner: `lib/store.ts` — localStorage `carpet_hub_store_number`; **no hardcoded `1234`/`1852`**
- Blank allowed; Master edits via **Settings → Store number** (session stays active)
- Session / active specialist / biometric only reject when both sides have different store numbers
- Login adopts `store_profiles` / specialist `store_number` when device store is unset
- Store-ops APIs require a live Supabase Auth session linked to `profiles` (Hub PIN bridge or phone OTP → `linkAuthUserToSpecialistProfile`); store scope from JWT claims, not client headers

## Mobile floor UX (Waves A–C)
- Floor job first: Dashboard = pace + checklist; no Super Admin quick-action strip, hamburger, or Admin Tools drawer
- Master setup tools live in Settings accordions/modals
- Dense bay/rotation rows; completed lists collapsed by default
- Remnant forms live in Settings; `/department` redirects to Floor

## Supervisor Invite & Onboarding
- Apply `20260810_supervisor_invite.sql`, `20260815_roster_invite_onboarding.sql`, **`20260815_unified_auth_token.sql`**, then **`20260815_roster_app_access.sql`** (backfill `pin_updated_at` so existing PINs show as app-Active), then **`20260815_roster_auth_link.sql`** (nullable `auth_user_id` / email claim so roster-only inserts and signup linking do not duplicate cards), then **`20260815_roster_insert_rls.sql`** (authenticated INSERT/SELECT so roster-only rows are visible after Hub PIN unlock)
- Master Admin: Roster → **Add Team Member** → Name, Role, Initial Department, optional Phone (contact / PIN-reset only). Pairing is **Pair Device via QR** on the specialist sheet (10-minute signed token; no SMS). Cards: Roster Only / Invited / Active. Roster-only and Invited rows can regenerate a QR.
- Self-service reset: AuthWall phone → `POST /api/auth/pin-reset/request` → `/auth/verify/[token]` (30m TTL)
- QR redemption: `/pair?t=` previews without burning; PIN + confirm burns `invite_token_hash`, hashes a 4–6 digit PIN, and mints Hub-bridge Auth with `store_number` in JWT `app_metadata`. After PIN: already-standalone → `/`; Chromium `beforeinstallprompt` → native install then `/`; iOS / blocked prompt → Add to Home Screen card then Continue to Floor (`/`). Capture owner is `lib/pwa-install.ts` (`display: standalone`, `start_url: /`).
- Owners: `lib/auth/invite-token.ts` (QR crypto), `lib/onboarding/qr-pair.ts` (issue/preview/redeem compose), `lib/onboarding/redeem-token.ts` (PIN + Hub JWT). SMS copy for PIN-reset remains `lib/invite.ts` + `lib/onboarding/pin-reset.ts`
- APIs: `POST /api/roster/members` (canonical roster-only insert), `POST /api/roster/pair` (issue QR), `POST /api/auth/redeem-invite` (public preview / PIN complete), `POST /api/auth/pin-reset/request`, `GET /api/auth/verify/[token]`, `GET|POST /api/auth/verify`
- Legacy `/invite/[token]` still redirects to `/auth/verify/[token]` (PIN-reset / old SMS tokens)

## Associate floor role
- Store Ops actor `associate`: read/complete dept rotations + locations; create exceptions via verify; **no** targets, invite, generate/reset, or Master Settings tools
- Nav: Master/DS Floor · Map · Roster · Settings; CSA **My Shift** + **Store Map** only (Downstock on Floor; specialty scans via Floor chips → `/?section=`)

## Department toggles · adaptive priority · showroom
- Apply `supabase/migrations/20260810_dept_priority_showroom.sql`
- Master toggles: Settings Department Overview (`DepartmentTargetsMatrix`, `departments.is_active`; Flooring default on)
- Adaptive draw: `manual_priority_count` + `last_completed_at` age + velocity/priority_override boost; after CARRIED_OVER, `pickSundayVelocityPrioritized` fills remaining slots from high/critical/override first; Store Map ★ Week assigns + bumps priority
- Showroom: `location_type=SHOWROOM_STACKOUT` + `audit_frequency_days`; dashboard Quick Touch card (not in weekly aisle draw)
- Store Map surfaces: `[ Standard Map | Velocity Heatmap ]` plus Snap Bay. Bay rows: Lucide status glyphs, `formatBayTag`, dual-pill Selling/Topstock (display-only); tap bay → `WalkTheFloorSheet` (walk + Snap Bay). Settings **Store Topology & Bay Setup** owns checkboxes, batch delete, duplicate prune, Add Bay, Bulk Generator, and `EditBayDrawer` (hotspot / priority lock / decay slider).

## Appliance categories (suite + sub)
- **Tables:** `appliance_catalog` + `appliance_scans` (not carpet_*). Apply `supabase/migrations/20260810_appliance_catalog_scans.sql` then `enable_rls_flagged_tables.sql` (RLS on appliances + `store_specialists` + verify all public tables)
- Top-level: Laundry · Refrigeration · Cooking / Ranges · Dishwashers · Microwaves / Venting
- Sub required on Quick-Add UPC link / floor scan / catalog (Laundry → Washer | Dryer | Combo / Unit)
- Types: `ApplianceCatalogItem`, `ApplianceScan` (`sub_category?`)
- APIs: `/api/appliances/catalog`, `/api/appliances/scans` (`?format=csv`)
- CSV export: **SUMMARY** (Item Number, Description, Category, Total Count Scanned, Locations Found) + **RAW DETAIL** (Category, Sub-Category, Item #, Serial #, Location, Scanned By, Scanned At, Store #)
- Online scans POST `/api/appliances/scans` (service role); failures surface as `Failed to save scan: …` (no silent offline success)
- **Continuous mode:** detect → POST immediately; no Submit button. Known SKU auto-logs; new/unlinked pauses on Quick-Add (sub_category) then logs + clears. Sticky **Session Total** counter at scanner top.
- **Scan log UX:** category accordion (collapsed default) → sub-category sections → SKU Qty cards; search auto-expands matches; 10 SKUs/page; sticky-free filter pills + search; Edit modal; `PATCH /api/appliances/scans`
